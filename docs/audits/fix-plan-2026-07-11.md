# Fix Plan — 2026-07-11 review remediation

Source: `docs/audits/architecture-review-2026-07-11.md` (68 verified findings). 64 fix specs in 21 sequenced batches. Class-fixes land before their instances.

## Execution posture (decided)
- Work on `main`, edit in the working tree, **no commits** (user didn't ask; 314 files already dirty — never `git add -A`).
- Editor is OPEN → do all `.gd` code + new tests + `.md` docs now, verify by careful re-read (revert hazard) + headless `-s` compile only.
- **Defer to a final editor-closed pass:** every `.tscn`/`.tres` edit (game.tscn, stray stupidbody, neutral_wildlife uid, chip icon bake), `--import` for new `.gd.uid` sidecars, the full GUT run, and every playtest-gated behavior.
- M1 (QuestTracker autoload split) stays **parked**. P0-3b overflow strip: ship code, validate in the closed pass.

## Strategy (from sequencer)
Work on main, not a branch: the repo is rpg/, the user works on main, and 314 dirty authoring files mean a branch would drag or strand in-progress scenes (F-HYG-6). All code/test/doc edits happen now under the open editor, verified by headless `-s` compilation only; a single editor-closed pass (Batch 21) collects everything the import lock blocks — `--import` for new-script .gd.uid sidecars, game.tscn/.tres reimports, the full GUT run, the windowed icon bake, and every playtest-gated behavior. Ordering fixes the class before its instances: the InputManager modal registry (T1-1) lands before all screen-touching fixes (F-HYG-5 mandate); CharacterStats.restamp_derived before its 3 callers; the zorkmids is_mirrored chokepoint before the loot debit; as_bool reads before pickup persistence; C8 before C36. P0 run-integrity goes earliest (Batches 2-6). The four god-files (GameState.gd, player.gd, npc.gd, InputManager.gd) each carry 3-7 findings across themes — I clustered npc.gd (Batches 14-15) and paired player.gd/GameState.gd touches, and every batch lists them in shared_files so the implementer re-reads the exact region before each Edit (buffered-editor revert hazard). Stage path-by-path, never git add -A.

## Batches
### Batch 1 — Process foundation: run-order + branch/staging runbook
**Fixes:** F-HYG-5, F-HYG-6  
**Why here:** Foundational, doc-only, no deps. F-HYG-5 encodes the editor-open/closed split and the mandatory Theme-1-before-other-screen-fixes ordering; F-HYG-6 fixes the git posture (stay on main, per-file path staging, never git add -A, serialize god-file edits). Every later batch executes inside these two runbook sections, so they must land first. Editor-open doc edits under docs/audits/editor_closed_session_runbook.md.  
**Verify:** code-only (Markdown). No compile/GUT surface. This batch defines the verification protocol the rest of the plan follows.

#### F-HYG-5 · [high] Whole-batch run-order runbook: all code+test+doc edits under the open editor, one editor-closed verify pass at the end
- **files:** docs/audits/editor_closed_session_runbook.md
- **change:** Author the batch execution order as a runbook section, because the environment splits sharply: EDITOR-OPEN phase (now) = every .gd/.tscn/.tres/.md edit + every new test, verified ONLY by headless `--check-only`/`-s` compile (no warnings, no runtime, no reimport — a buffered .tscn/.tres the editor holds can silently revert, so re-read before relying). EDITOR-CLOSED phase (once, at the end) = the ONLY place that may run `--import` (sidecars per F-HYG-1 + reimport), the GUT suite, and manual playtest. Ordering within the open phase, mirroring the review's own sequencing (doc lines 452-461): (1) P0-1 empty game.tscn starting_unlocks + its scene-contract test; (2) P0-2 profile_active flag; (3) Theme 1 — collapse to InputManager._modal_screens() single registry + rewrite Player._close_open_modals + extend test_modal_registry.gd (do this BEFORE any other screen-touching fix so C40/C48/C23/C26 ride the new chokepoint); (4) Themes 2 & 3 batched (liveness gate + is_holdable/is_mirrored chokepoint); (5) P0-3 overflow strip; (6) Themes 4-12 severity-ranked. Doc edits (F-HYG-2/3/4) land in the same diff as their code fix, LAST within each fix. The editor-closed pass then runs: --import → missing-uid grep → full GUT (`--headless --path . -s addons/gut/gut_cmdln.gd -gexit`, GUT 9.x assert_lte/gte, no Player/NPC _ready() in units) → editor-open reload to check warnings → playtest the modal/death/cutscene/hotbar seams that only playtest can prove. Encode which findings are compile-checkable now vs deferred to that pass.
- **test:** none: process runbook, no code surface.
- **docs:** This finding IS the runbook (docs/audits/editor_closed_session_runbook.md — 'Batch run-order' + 'Editor-closed verify pass' sections).
- **verification:** code-only · **effort:** S
- **risk:** Theme 1 MUST precede the other screen-touching fixes or they will be written against the old hand-lists and re-collide. Nothing in the open phase proves runtime — do not mark any liveness/modal/save-migration fix 'verified' until the editor-closed GUT+playtest pass. shared god-files (see F-HYG-6) are edited by multiple ordered steps; serialize them.

#### F-HYG-6 · [high] Stay on main; per-file staging protocol so fixes never sweep the 314 dirty authoring files
- **files:** docs/audits/editor_closed_session_runbook.md
- **change:** Decision: work on main, do NOT branch. The git repo is the rpg/ subdir (memory 'git-repo-is-rpg-subdir'); the user 'works on main' (CLAUDE.md Git) and has 314 dirty files of in-progress scene/resource authoring. A branch would either drag those uncommitted edits along or strand them, and switching back risks reimport churn while the editor is open. Instead codify a collision-avoidance protocol in the runbook: (1) touch ONLY the files each fix names — never `git add -A`/`git add .`/`git commit -a` (memory 'git-add-sweeps-shared-file-edits'); stage path-by-path (`git add <exact file> <its .gd.uid>`). (2) The god-files edited by MULTIPLE themes — managers/GameState.gd (C17/C43/C44/C-P1), scripts/player/player.gd (P0-2/C9/C14/C18/C26/C38/C39), scripts/npc/npc.gd (P0-4/C7/C8/C32/C33/C36), managers/InputManager.gd (Theme 1) — are the collision hot-spots: apply all edits to one god-file in a single serialized session, re-Grep/re-Read the exact region immediately before each Edit (buffered-editor reverts), and never blind-overwrite. (3) Before staging, `git diff --stat -- <file>` to confirm no unrelated authoring hunk got captured; if a shared .tscn/.tres shows edits you didn't make, that's the user's in-progress work — leave it unstaged. (4) `git diff --check` before every commit; commit messages via -F file (memory 'commit-msg-via-file'), Co-Authored-By trailer, no push without explicit request.
- **test:** none: process runbook, no code surface.
- **docs:** This finding IS the runbook addition (docs/audits/editor_closed_session_runbook.md — 'Branch policy & per-file staging').
- **verification:** code-only · **effort:** S
- **risk:** The god-files are the single biggest collision surface across the whole remediation — GameState.gd/player.gd/npc.gd/InputManager.gd each carry 3-7 findings from different planners; if two fixes edit the same function out of order, the second Edit's old_string won't match (safe fail) or silently lands on stale text (dangerous). Serialize per-file and re-read. Never stage a shared authored .tscn/.tres the user is mid-edit on.

### Batch 2 — game.tscn integrity: empty starting_unlocks + remove stray stupidbody
**Fixes:** F-P0-1, F-REM-C56  
**Shared god-files:** scenes/game.tscn  
**Why here:** Both are text edits to scenes/game.tscn with identical .tscn buffered-revert risk, so co-locating them means one re-read/verify of the same file rather than two touches under the open editor. F-P0-1 is the highest-consequence P0 (New Game must grant zero abilities — the chip economy). F-REM-C56 deletes the debug stupidbody node + its orphaned ext_resource (grep-confirmed sole refs at lines 11/60). No deps. Ships with tests/test_new_game_contract.gd.  
**Verify:** NOW: test_new_game_contract.gd compiles via headless -s (instantiate-without-add_child, no _ready). DEFERRED (editor-closed): game.tscn reimport, GUT run of the contract test, and re-grep to confirm the empty-array + node-deletion persisted (editor may re-serialize). See open_decisions for text-edit vs editor-dock-deletion.

#### F-P0-1 · [critical] Empty game.tscn's starting_unlocks so New Game grants zero abilities
- **files:** scenes/game.tscn, tests/test_new_game_contract.gd
- **change:** In scenes/game.tscn line 38, replace `starting_unlocks = Array[String](["laser_sight", "wall_climb", "air_dash", "slide", "grapple_hook", "grapple"])` with `starting_unlocks = Array[String]([])` (empty, matching Player.tscn:55). Keep the property present-but-empty rather than deleting the line, so an editor re-serialize can't reintroduce a stale value and the override reads unambiguously. This restores the microchip-economy contract: a fresh game starts with ZERO abilities and each is paid for at a ChipInstaller. Note the removed list also contained the bogus id `grapple_hook` (not an AbilityRegistry id; `grapple` is the real one) — a second reason the override was wrong. NEW test file tests/test_new_game_contract.gd: `var packed := load("res://scenes/game.tscn") as PackedScene` then `var g := packed.instantiate()` (instantiate WITHOUT add_child so no _ready runs — the test_level_door_prefab idiom); guard the reimport-empty case (`if g == null: return`); `var p := g.get_node_or_null("Player")`; `assert_true(p != null)`; `assert_true(p.starting_unlocks.is_empty(), "game.tscn must not pre-grant abilities — the chip economy requires zero starting unlocks")`; then a forward-guard loop: `var valid := load("res://scripts/components/abilities/ability_registry.gd").ids()` and `for id in p.starting_unlocks: assert_true(valid.has(id), "starting_unlocks id '%s' is not a real AbilityRegistry id" % id)`; `g.free()`.
- **test:** tests/test_new_game_contract.gd (new): instantiate-without-add_child game.tscn, assert Player.starting_unlocks.is_empty() and every present id is in AbilityRegistry.ids(). Reimport-empty guarded. No _ready runs (never add_child'd).
- **docs:** docs/CURRENT_ARCHITECTURE.md: confirm the 'fresh game = zero abilities, chips are paid installs' contract names game.tscn as a covered surface. Update the memory note microchip-upgrade-installs if it implies game.tscn was already clean.
- **verification:** gut-editor-closed · **effort:** S
- **risk:** scenes/game.tscn is a .tscn the user may have open in the editor — a buffered save can silently revert this edit (per project memory 'writes can revert'). After editing, re-grep game.tscn:38 to confirm the empty array persisted; if the editor is open on Game.tscn the user must apply/close it. No hot path. Isolated scene file — no god-file collision.

#### F-REM-C56 · [medium] Delete stray `stupidbody` node + its orphaned ext_resource from scenes/game.tscn
- **files:** scenes/game.tscn
- **change:** Remove the debug leftover flagged as B-F63. Delete exactly three lines: (a) line 11 `[ext_resource type="PackedScene" uid="uid://cyn56c0cfkevl" path="res://scenes/stupidbody.blend" id="6_p57ef"]` and (b) lines 60-61, the node block:
`[node name="stupidbody" parent="." unique_id=506447465 instance=ExtResource("6_p57ef")]`
`transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0.5148362, 5951.2617, 3.0735283)`
Verified `6_p57ef` is referenced ONLY at lines 11 and 60 (grep confirmed), so removing both leaves no dangling ExtResource ref. `stupidbody` is a leaf node (file ends at line 62, no children under it). Do NOT touch the `stupidbody.blend` asset on disk (out of scope; leave it). Keep every other node/ext_resource id intact — do not renumber.
- **test:** none: scene-authored. Optional cheap guard when the editor is next available — a one-liner in a GUT scene-wiring test asserting `load("res://scenes/game.tscn").instantiate()` has no child named "stupidbody" — but this is not required and adds a scene-instantiate cost; skip unless bundling with other game.tscn coverage.
- **docs:** none
- **verification:** scene-authored · **effort:** S
- **risk:** HIGH revert risk: game.tscn is very likely open/buffered in the running editor, and a text-tool edit to a scene the editor has in memory can be silently clobbered on the editor's next save (known writes-can-revert failure). SAFEST path: have the user delete the `stupidbody` node in the editor's Scene dock (which auto-drops the orphaned ext_resource) rather than a text edit. If done by text edit, do it with the editor closed, or re-Read the file afterward AND confirm with the user before relying on it. Byte-order-sensitive .tscn — remove whole lines only, never partial.

### Batch 3 — P0-2: profile_active promotes an in-memory New-Game run on CHECKPOINT_FRESH death
**Fixes:** F-P0-2  
**Shared god-files:** managers/GameState.gd, scripts/player/player.gd  
**Why here:** P0 run-integrity, earliest after the trivial scene fix. First touch of god-files GameState.gd (add profile_active beside loaded; set/clear at load_from_disk + reset_for_new_game) and player.gd (promote loaded at the CHECKPOINT_FRESH reload) plus start_menu.gd. No deps. Isolated commit boundary; source-string + off-tree GameState tests in test_game_save.gd.  
**Verify:** NOW: compile via -s; off-tree GameState tests (reset clears flag, disk-load sets it) and source-string asserts on player.gd/start_menu.gd. DEFERRED: the in-tree die→reload→apply path is playtest-only (Player._ready can't run in a unit test).

#### F-P0-2 · [critical] Add profile_active flag so a New-Game CHECKPOINT_FRESH death reloads the run, not a fresh build
- **files:** managers/GameState.gd, scripts/ui/start_menu.gd, scripts/player/player.gd, tests/test_game_save.gd
- **change:** Root cause: `GameState.loaded` is true ONLY after load_from_disk, so a New-Game session has loaded=false; the RELOAD_CHECKPOINT_FRESH death mode reloads the scene with no load_from_disk, and player.gd:507-635 gates every restore (unlocks/perks/xp/level/inventory/money/reputation/respawn) on `loaded` — so the fresh Player reseeds a DEFAULT build and the next milestone autosave overwrites the run on disk. Fix (minimal, preserves the fresh-boot seed path): (1) managers/GameState.gd — add `var profile_active: bool = false` beside `loaded` (doc: 'a real run is authoritative in memory — set by a disk load OR character creation; unlike `loaded` it is not reset by a scene reload, so a New-Game session survives a CHECKPOINT_FRESH death reload'). Set `profile_active = true` next to `loaded = true` at the end of load_from_disk (~line 257). Set `profile_active = false` inside reset_for_new_game (~line 652, next to `loaded = false`). (2) scripts/ui/start_menu.gd _on_character_confirmed (~line 159, after the stat-stamp loop, before _start_game): add `GameState.profile_active = true  # a created character IS an authoritative run even before the first autosave`. (3) scripts/player/player.gd _on_death_sequence_done, the RELOAD_CHECKPOINT_FRESH branch (~line 2379-2381): before `get_tree().reload_current_scene()`, insert `if GameState.profile_active: GameState.loaded = true  # promote the in-memory run so the fresh Player APPLIES it (unlocks/xp/money/inventory) instead of reseeding a default build (P0-2)`. Update that branch's comment (currently claims 'the in-memory profile carries over' — now actually true). Do NOT gate player._ready restores on profile_active (that would change fresh-boot behaviour: loadout-money clobber + skipped initial respawn seed). Promoting only at the death reload is safe because by then GameState holds the real run (last-autosave state: unlocks/money/has_respawn all valid).
- **test:** tests/test_game_save.gd (extend, off-tree gs = load(GAMESTATE_PATH).new()): (a) test_profile_active_cleared_by_reset — gs.profile_active=true; gs.reset_for_new_game(); assert_false(gs.profile_active). (b) test_profile_active_set_by_disk_load — populate gs, save_to_disk(TMP_SAVE), fresh gs2=load().new(); assert_false(gs2.profile_active) before; gs2.load_from_disk(TMP_SAVE); assert_true(gs2.profile_active) and assert_true(gs2.loaded). (c) source-string assertions (the test_npc_facade_contract idiom): read player.gd, assert it contains `GameState.profile_active` and `GameState.loaded = true` within the CHECKPOINT_FRESH handling; read start_menu.gd, assert it contains `GameState.profile_active = true`. Cannot run Player._ready in a unit test, so the in-tree apply stays playtest-verified. Use assert_true/assert_false only.
- **docs:** managers/GameState.gd doc-comment on `loaded` (line 49) and the new `profile_active` var explaining the disk-read vs authoritative-run split. player.gd:2379 branch comment. docs/CURRENT_ARCHITECTURE.md save-model section: note CHECKPOINT_FRESH now promotes the in-memory profile via profile_active.
- **verification:** gut-editor-closed · **effort:** M
- **risk:** HOT/SHARED god-files: managers/GameState.gd and scripts/player/player.gd are touched by multiple themes — coordinate landing to avoid edit collisions; keep the diff to the exact lines named. Behavioural risk: setting GameState.loaded=true at death also makes GameRoot reload current_level_path (already set during the session) and applies the saved clock — both correct for a checkpoint reload. Dev-boot straight into game.tscn (no StartMenu, no disk save) leaves profile_active=false, so it still seeds fresh (unchanged). Not a per-frame path.

### Batch 4 — P0-3a: re-home unplaced stacks when grid cells free (corpse-drain)
**Fixes:** F-P0-3a  
**Shared god-files:** scripts/inventory/character_inventory.gd  
**Why here:** P0 run-integrity, isolated non-god file (character_inventory.gd _rehome_unplaced on removal). Must land before P0-3b, which depends on this guarantee and only handles the residual (a stack too big for even an empty grid). No deps. Off-tree GUT coverage in test_character_inventory_grid.gd.  
**Verify:** NOW: compile via -s; off-tree bare .new() test (rehome-on-remove, is_empty after drain). GUT run deferred to editor-closed pass.

#### F-P0-3a · [high] Re-home unplaced stacks when grid cells free so overflowed corpse loot drains
- **files:** scripts/inventory/character_inventory.gd, tests/test_character_inventory_grid.gd
- **change:** CharacterInventory keeps stacks that don't fit the grid unplaced (x<0). A LootableCorpse seeds its wallet as a coin tile with the grid OFF, then LootScreen enable_grid()s the copy; if the coin overflows a full loot grid it stays unplaced, GridInventoryView renders no tile for it, so it can't be taken → is_empty() never reaches true → the ragdoll's linger-until-drained fade never fires and the corpse stays a lootable-but-unlootable soft-lock (worsened by the 2026-07-09 tetris-for-everyone rollout). Fix: auto-place unplaced stacks the moment cells free. Add helper `func _rehome_unplaced() -> bool` (near _refit_placement, ~line 314): iterate `_stacks`; for each whose `_grid.placement_of(s["key"]).is_empty()` and item != null, `var slot := _grid.find_free_slot(it.grid_width, it.grid_height, true)`; if `slot["found"]` then `_grid.place(s["key"], slot.x, slot.y, slot.w, slot.h)` and mark any=true; return any. It only claims FREE cells (never evicts). Call it (guarded by `_grid_enabled`) right before the `changed.emit()` in remove() (~line 195, inside `if removed > 0:`) and in remove_stack() (~line 217, before its emit). transfer_to() routes through remove() so it is covered. The single existing changed.emit() drives the view refresh; no new signal. No edit needed to lootable_corpse.gd/ragdoll — once the coin is takeable, removing it empties _stacks → is_empty() true → fade.
- **test:** tests/test_character_inventory_grid.gd (extend, bare .new() off-tree — CharacterInventory has no _ready): test_removing_a_placed_stack_rehomes_an_unplaced_one — inv.enable_grid(2,2); big=_item(&"big",2,2); assert_eq(inv.add(big),1); coin=_item(&"coin",1,1); inv.set_item_count(coin,1) (kept unplaced, the corpse-wallet-overflow shape); read placed_contents(), find the coin row, assert_true(int(row.x) < 0) 'coin unplaced while grid full'; inv.remove(big); re-read placed_contents(), assert_true(int(row.x) >= 0) 'freeing cells re-homes the coin'. Also assert inv.is_empty() becomes true after inv.remove(coin). Use assert_true/assert_eq (avoid assert_le/ge).
- **docs:** Nearby comment on _rehome_unplaced explaining the corpse-drain contract it protects. docs/AUTHORING_GUIDE.md or the inventory-tetris memory note: unplaced overflow now self-heals when room frees. Note the residual limit: a stack too big for even an EMPTY grid stays unplaced until F-P0-3b's strip ships.
- **verification:** gut-editor-closed · **effort:** M
- **risk:** Isolated non-god file. _rehome_unplaced runs only on removal (not per-frame); O(stacks x grid) but bounded and infrequent. Edge NOT covered: a loot item larger than the whole grid can never be placed → still needs F-P0-3b (strip) or a drain-tolerance fallback for the ragdoll to fade. Ensure find_free_slot's returned w/h are used (rotation) rather than re-deriving from the item.

### Batch 5 — P0-3b: click-only overflow strip in GridInventoryView (B-F24)
**Fixes:** F-P0-3b  
**Why here:** Depends on F-P0-3a (which keeps the strip usually empty). Larger self-contained UI change to grid_inventory_view.gd (render x<0 rows in a strip, hit-test to activate_requested/drop_requested; LootScreen/InventoryScreen need zero host changes). Kept as its own commit because it is L-effort and the strip layout/hit-test is playtest-gated.  
**Verify:** NOW: compile via -s; source-string asserts that _sync_tiles no longer unconditionally skips x<0 and _gui_input routes strip clicks. DEFERRED: strip layout/coords + click hit-test are playtest-only under the open editor. See open_decisions on shipping-now vs deferring the strip.

#### F-P0-3b · [high] Render unplaced stacks in a click-only overflow strip in GridInventoryView
- **files:** scripts/ui/grid_inventory_view.gd, tests/test_inventory_screen.gd
- **change:** Complete the planned B-F24 overflow strip so unplaced stacks (x<0 rows, including a coin too big to ever fit — the case F-P0-3a can't re-home) are visible and interactive. In scripts/ui/grid_inventory_view.gd: (1) _sync_tiles (line 374) and _key_at_cell (line 175) currently `continue` on `x<0` — instead lay unplaced rows out in a horizontal strip BELOW the grid (y = rows*_cell_px + gap), one _cell_px-sized tile each, left-to-right. (2) Reserve the strip's height: in _recompute_cell (line 140) add `_cell_px` (one strip row) to custom_minimum_size.y when any row has x<0. (3) Hit-test the strip in _gui_input (line 196): a left-click on a strip tile emits activate_requested (take/equip — routes through LootScreen._on_source_activate exactly like a grid tile), a right-click emits drop_requested(item, key). CLICK-ONLY: no drag into/out of the strip (skip _begin_press/_start_drag for strip tiles), which keeps the drag machinery untouched. (4) Draw a faint 'overflow' divider above the strip in _draw. Because take/drop already flow through activate_requested/drop_requested, LootScreen and InventoryScreen need ZERO host changes. Once the coin is takeable, the corpse drains and the ragdoll fades.
- **test:** tests/test_inventory_screen.gd (or a new tests/test_grid_overflow_strip.gd): source-string contract (the view's hit-test is in-tree/mouse-driven, not unit-runnable) — read grid_inventory_view.gd and assert it no longer unconditionally skips x<0 in _sync_tiles (assert it references an unplaced/strip layout branch) and that _gui_input routes strip clicks to activate_requested/drop_requested. Plus, if a GridInventoryView can be built with .new() off-tree without _ready side effects, bind a CharacterInventory holding one unplaced stack and assert a strip tile child is created (guard is_visible_in_tree not required). In-tree click/layout is playtest-gated.
- **docs:** grid_inventory_view.gd class header (lines 4-21) currently says the view renders only placed stacks — update to describe the overflow strip. docs/AUTHORING_GUIDE.md inventory section + the inventory-tetris memory note: unplaced overflow is now shown in a click-only strip. Mark B-F24 as shipped wherever it's listed as planned.
- **depends_on:** F-P0-3a
- **verification:** playtest-gated · **effort:** L
- **risk:** Larger UI change in a self-contained file (no god-file), but editor-open means no playtest this session — the strip's layout/hit-test must be verified editor-closed. Keep the strip click-only to avoid entangling the drag state machine (_dragging/_drag_key). Watch the strip's coordinate space vs the overlay (kept as last child). Complements, does not replace, F-P0-3a (which keeps the strip usually empty by auto-re-homing).

### Batch 6 — P0-4: run noise/corpse sensing in the has-target branch while UNAWARE
**Fixes:** F-P0-4  
**Shared god-files:** scripts/npc/npc.gd  
**Why here:** P0 run-integrity, third instance of the both-branches lesson. First touch of god-file npc.gd: extract _scan_distractions, add _react_distraction, call it before _react_music in the has-target branch (region ~1789-1858). No deps, but must coordinate with the later npc.gd batches (14/15) — it edits a distinct sub-region. Source-string + off-tree null-guard test in test_npc_facade_contract.gd.  
**Verify:** NOW: compile via -s; source-string asserts the has-target path calls _react_distraction and the UNAWARE self-gate exists; off-tree _react_distraction(0.1) no-error. DEFERRED: the real in-tree LOS/noise scan is playtest-gated (_senses null off-tree).

#### F-P0-4 · [high] Run noise/corpse sensing in the has-target branch while UNAWARE (both-branches fix)
- **files:** scripts/npc/npc.gd, tests/test_npc_facade_contract.gd
- **change:** _react_unaware (the ONLY consumer of the &"noise" channel + Corpse body-discovery) runs only in the no-target branch (npc.gd:1805). But NpcTargeting binds a target by pure proximity (hostile within sight_range, no LOS/perception gate), so a hostile guard holds the player as _target the instant they enter sight_range even while perception is UNAWARE — the has-target branch never scans noise/corpses, so thrown decoys and hidden bodies do nothing exactly in range. Same shape as the already-fixed _react_music both-branches lesson. Fix: (1) Extract the throttled scan block from _react_unaware (current lines 1897-1909: the `_distraction_scan_t` decrement, `_loudest_noise()`->investigate_point, and the corpse `_nearest_visible_corpse()`->_discover_corpse) into `func _scan_distractions(delta: float, noise_on: bool, corpse_on: bool) -> void` (do NOT include the sense() call at 1892 nor the _was_distracted/bark bookkeeping at 1912-1917 — those stay in _react_unaware). Have _react_unaware call `_scan_distractions(delta, noise_on, corpse_on)` in place of the extracted lines. (2) Add `func _react_distraction(delta: float) -> void` mirroring _react_music: early-return if `_perception == null or _dead or hp <= 0.0 or is_fleeing() or is_following()`; early-return if `_perception.state != Perception.State.UNAWARE`; compute `noise_on = _noise_initiates_on() and _perception.hearing`, `corpse_on = _body_discovery_on()`; if `noise_on or corpse_on: _scan_distractions(delta, noise_on, corpse_on)`. (3) In _physics_process's has-target branch, call `_react_distraction(delta)` immediately BEFORE `_react_music(delta)` at line 1858 (so a fired decoy escalates to INVESTIGATING and _react_music then self-drops the radio face). The has-target branch already calls _perception.sense() (line 1820) and handles INVESTIGATING barks (1832-1833), so _scan_distractions must NOT duplicate sense() or search barks. Escalation to INVESTIGATING with _target still set routes the executor to GoapActionSearch (off last_known_position), peeling the guard toward the decoy — the intended stealth behaviour.
- **test:** tests/test_npc_facade_contract.gd (extend — source-string idiom, since the scan is in-tree/LOS-driven and playtest-verified): read scripts/npc/npc.gd source and assert (a) it contains `func _scan_distractions(` and `func _react_distraction(`; (b) the has-target path calls `_react_distraction(` (pins that distraction sensing runs in BOTH branches — the exact P0-4 regression); (c) _react_distraction contains `Perception.State.UNAWARE` (the self-gate). Also `var npc = load("res://scripts/npc/npc.gd").new()` (no add_child, no _ready) and `assert_true(npc.has_method("_react_distraction"))` / `assert_true(npc.has_method("_scan_distractions"))`; call `npc._react_distraction(0.1)` and assert it does not error with _perception null (early-return guard). npc.free(). The pure gates (Corpse.noticeable / NoiseSource.audible) keep their existing unit coverage.
- **docs:** npc.gd comments: the new _scan_distractions and _react_distraction doc-comments (explaining the proximity-target/UNAWARE rationale, mirroring the _react_music comment at 1923-1932); update _react_unaware's header (1861-1868) to note the scan is now shared. scripts/npc/goap/README.md or the 'idle/UNAWARE reactions run BOTH physics branches' memory note: add distraction sensing as the third both-branches case.
- **verification:** playtest-gated · **effort:** M
- **risk:** HOT/SHARED god-file scripts/npc/npc.gd (multiple themes edit it and _physics_process is the per-frame AI hot path) — keep the diff to the extraction + the two new methods + one call line; coordinate landing order with other npc.gd themes. Correctness watch: _scan_distractions must not re-run _perception.sense() (would double-sense / clobber is_hostile set at 1820); the UNAWARE gate must stay so a genuinely-alerted foe isn't distractible; place the call before _react_music so a decoy outranks music. Off-tree _senses is null so _loudest_noise/_nearest_visible_corpse return null (safe) — the real scan is in-tree, hence playtest-gated.

### Batch 7 — T1-1: collapse InputManager modal hand-lists into one lazily-cached _modal_reg
**Fixes:** F-T1-1  
**Shared god-files:** managers/InputManager.gd  
**Why here:** The Theme-1 chokepoint and a hard prerequisite: F-HYG-5 mandates this single-source registry lands BEFORE any other screen-touching fix so those fixes are written against the new helpers, not the stale hand-lists. Rewrites _modal_screens/any_modal_open/any_pausing_open/gameplay_suppressed onto _modal_reg and adds close_all_modals(). gameplay_suppressed truth set must stay byte-identical (movement/fire gate). No deps.  
**Verify:** NOW: compile via -s (the hot-path gate). DEFERRED: the drift/truth-set assertions live in F-T1-5 (Batch 8), run editor-closed.

#### F-T1-1 · [high] Collapse the 3 InputManager modal hand-lists into one lazily-cached tagged registry + a close-sweep
- **files:** managers/InputManager.gd
- **change:** Replace the three parallel hand-lists (gameplay_suppressed L101, _modal_screens L107-108, any_pausing_open L123-124) with ONE authored source. Add members: `var _modal_reg: Array[Dictionary] = []` and `var _modal_screens_cache: Array = []`. Add `func _ensure_modal_reg() -> void:` that early-returns when `not _modal_reg.is_empty()`, else assigns `_modal_reg` to a 12-row literal of `{screen = <Autoload>, pausing = <bool>}` in exactly this order/flags: OptionsMenu=false, InventoryScreen=false, LootScreen=false, ShopScreen=true, StatsScreen=false, ReputationScreen=false, LevelUpScreen=true, RespecScreen=true, HealScreen=true, ChipInstallScreen=true, ChessScreen=true, QuestJournal=false — then builds `_modal_screens_cache` by looping `_modal_reg` appending `e.screen`. Lazy because screen autoloads are registered AFTER InputManager in project.godot [autoload], so they don't exist at _ready(); every query runs at runtime. Rewrite: `_modal_screens()` -> `_ensure_modal_reg(); return _modal_screens_cache`. `any_modal_open(exclude)` -> `_ensure_modal_reg()` then `for e in _modal_reg: var m: Object = e.screen; if m != exclude and m.is_open(): return true` / `return false`. `any_pausing_open()` -> `_ensure_modal_reg()` then `for e in _modal_reg: if e.pausing and e.screen.is_open(): return true` / `return false`. `gameplay_suppressed()` -> `return any_modal_open() or CutscenePlayer.is_active() or NameEntryDialog.is_open()` (truth set stays byte-identical to the old OR-chain: the 12 registry screens + the two control-only suppressors). ADD new `func close_all_modals() -> void:` that does `for m in _modal_screens(): if m.is_open(): m.close()` then `if NameEntryDialog.is_open(): NameEntryDialog.close()`. Update the block comments to describe the single registry + that a new screen = one _modal_reg row + its project.godot autoload.
- **test:** Covered by F-T1-5. New assertions there: gameplay_suppressed() is true when EACH registry screen (looped) is the only one open, and true for CutscenePlayer/NameEntryDialog, false when none — pinning the byte-identical truth set.
- **docs:** docs/CURRENT_ARCHITECTURE.md — update the modal-registry contract section to say InputManager._modal_reg is the single authored list all four consumers (suppression, stack-open guards, pausing query, close-sweep) derive from; adding a screen = one row + one autoload. Also update the block comments in InputManager.gd itself.
- **verification:** compile-check · **effort:** M
- **risk:** InputManager.gd is a shared god-file (Theme 2 also touches input gating). gameplay_suppressed() is THE load-bearing per-frame gate with ~12 callers (player movement/jump/fire, reload, scope, crouch, grapple, claim, pet, takedown) — its truth set MUST stay byte-identical or movement/fire gating breaks. It is also a hot path: the lazy cache is what keeps it allocation-free after the first call (do NOT rebuild the Array[Dictionary] per call). Autoload identities are app-lifetime stable, so caching refs is safe.

### Batch 8 — T1 consumers: registry close-sweep, inline open-gates, quicksave gate + drift test
**Fixes:** F-T1-2, F-T1-3, F-T1-4, F-T1-5  
**Shared god-files:** scripts/player/player.gd, managers/GameState.gd, managers/InputManager.gd  
**Why here:** All depend on F-T1-1. One coherent 'route every consumer through the registry' commit: Player._close_open_modals→close_all_modals (fixes Chess/NameEntry omission), OptionsMenu/LootScreen inline gates→any_modal_open(self), quicksave/quickload gated on gameplay_suppressed + pre-reload close_all_modals, and the extended test_modal_registry pinning size=12/membership/pausing/close-sweep. Re-touches player.gd and GameState.gd — re-read both regions first (Batch 3 already edited them).  
**Verify:** NOW: compile via -s; source-string asserts across player.gd/options_menu.gd/loot_screen.gd/GameState.gd. DEFERRED: the registry drift test + death-with-Chess-open / F9-under-backpack behaviors are GUT+playtest editor-closed.

#### F-T1-2 · [high] Rewrite Player._close_open_modals as the single registry close-sweep (fixes ChessScreen + NameEntryDialog omission)
- **files:** scripts/player/player.gd
- **change:** Replace the body of `_close_open_modals()` (player.gd:2257-2279, the 11 hand-written `if X.is_open(): X.close()` blocks that omit ChessScreen and NameEntryDialog) with a single call: `InputManager.close_all_modals()`. Update the doc comment above it (2253-2256) to say it delegates to InputManager.close_all_modals() — the one registry-driven sweep — and drop the now-stale 'one exhaustive list' claim and the QuestJournal/ChipInstall 'was missing here' breadcrumbs (they're structurally impossible to miss now). Keep the two call sites unchanged (player.gd:2143 dialogue-death abort path, player.gd:2394 post-cinematic respawn). This closes the pet-naming box on death so it no longer floats over the death cinematic and revive, and closes ChessScreen.
- **test:** Covered by F-T1-5 (source-string assert that _close_open_modals routes through InputManager.close_all_modals()). Runtime death-with-Chess-open / death-with-name-box-open is playtest-only (can't run Player._ready in a unit test per CLAUDE.md).
- **docs:** 
- **depends_on:** F-T1-1
- **verification:** playtest-gated · **effort:** S
- **risk:** player.gd is a shared god-file that Theme 2 (death/liveness gate) and Theme 3 also edit — touch ONLY _close_open_modals and its comment; do not restructure the surrounding death sequence. NameEntryDialog.close() clears its on_confirm callback WITHOUT emitting `cancelled` (name_entry_dialog.gd:80-86) — correct for death teardown (the pet-claim must not complete), but verify no code path relies on `cancelled` firing here (none found).

#### F-T1-3 · [medium] Route the two surviving inline open-gates (OptionsMenu, LootScreen) through any_modal_open(self)
- **files:** scripts/ui/options_menu.gd, scripts/ui/loot_screen.gd
- **change:** options_menu.gd:71 — replace the long inline OR-chain (which omits ChessScreen + ChipInstallScreen) with `if _is_open or DialogueManager.is_active() or NameEntryDialog.is_open() or InputManager.any_modal_open(self):`. Keep the explicit NameEntryDialog guard because it is NOT in _modal_reg (it's a control-only suppressor) yet OptionsMenu must still refuse to open over the real-time name box. loot_screen.gd:137 — replace the inline OR-chain (omits Stats/Reputation/Journal/ChipInstall/Chess) with `if _is_open or DialogueManager.is_active() or InputManager.any_modal_open(self):`. In both, `any_modal_open(self)` excludes the screen itself by identity so its own guard won't self-block. Update the trailing comments to note the guard now derives from the shared registry (matches shop/heal/level_up/respec/chess/chip_install which already route this way).
- **test:** Covered by F-T1-5 source-string asserts: options_menu.gd and loot_screen.gd each contain `InputManager.any_modal_open(self)` (extends the existing test_guards_route_through_the_shared_helpers pattern).
- **docs:** 
- **depends_on:** F-T1-1
- **verification:** compile-check · **effort:** S
- **risk:** any_modal_open already exists and is used by 6 other screens, so behavior is well-established. Only behavior delta: LootScreen now also refuses to open over the real-time Pip-Boy tabs (Stats/Reputation/Journal) and OptionsMenu/Loot refuse over Chess/ChipInstall — intended tightening, not a regression. Low risk; both are self-contained UI scripts, not god-files.

#### F-T1-4 · [high] Gate quicksave/quickload behind gameplay_suppressed + close modals before any scene reload
- **files:** scripts/player/player.gd, managers/GameState.gd
- **change:** Part A (player.gd) — at the top of `_update_save_input()` (player.gd:1480, called at :1747 BEFORE the suppression gate at :1751) add an early return: `if InputManager.gameplay_suppressed():\n\treturn`. This stops F5/F9 from firing while a NON-pausing modal (Inventory/Stats/Loot/Options — which don't pause the tree, so _physics_process still runs) is open; the pausing screens already block it by pausing. Update the doc comment (1475-1479) to note the gate now also covers the real-time overlays, not just paused transaction screens. Part B (GameState.gd) — in `_load_and_reload()` (GameState.gd:544-552), inside the existing `if is_inside_tree() and get_tree() != null:` block, add `InputManager.close_all_modals()` as the FIRST line (before `Engine.time_scale = 1.0` and `get_tree().reload_current_scene()`), so any autoload screen bound to a soon-freed scene node releases its ref before the reload. Placing it inside the in-tree branch keeps the off-tree unit-test path (test_save_slots.gd:94) untouched. Add a one-line comment explaining the pre-reload teardown.
- **test:** Covered by F-T1-5 source-string asserts: player.gd _update_save_input body contains `gameplay_suppressed()`; GameState.gd _load_and_reload body contains `close_all_modals()`. Runtime (F9 under an open backpack no longer reloads) is playtest-only — _update_save_input runs inside Player._physics_process which can't be unit-tested per CLAUDE.md.
- **docs:** 
- **depends_on:** F-T1-1
- **verification:** playtest-gated · **effort:** S
- **risk:** GameState.gd is a shared god-file (P0-2 and the save-fidelity theme both edit it) — touch ONLY _load_and_reload's in-tree branch. Confirm InputManager (autoload #3) resolves from GameState (autoload #9): it does at runtime. Part A gates BOTH quicksave and quickload (consistent immersive-sim live-gameplay-only loop); saving-under-menu was harmless but loading-under-menu is the stale-ref danger. Today the only live reload-under-open-modal path is quickload from the player poll (covered by Part A); Part B is defense-in-depth for the not-yet-wired load-slot menu and any future reload caller.

#### F-T1-5 · [medium] Extend the modal-registry drift test to Chess + ChipInstall, the close-sweep, and the quicksave gate
- **files:** tests/test_modal_registry.gd
- **change:** Replace the fixed after_each() (lines 14-21) with a generic reset that loops the registry: `for s in InputManager._modal_screens():\n\ts.set(&"_is_open", false)` — future-proof, covers Chess/ChipInstall (all 12 screens expose `_is_open`). Add test funcs: (1) test_registry_size_and_membership: `var screens := InputManager._modal_screens(); assert_eq(screens.size(), 12, ...); assert_true(screens.has(ChessScreen)); assert_true(screens.has(ChipInstallScreen)); assert_true(screens.has(QuestJournal))` — pins the historically-forgotten screens so the next new screen forces a deliberate test edit. (2) test_gameplay_suppressed_fires_for_every_registered_modal: assert_false(InputManager.gameplay_suppressed()) with none open; then `for s in InputManager._modal_screens(): s.set(&"_is_open", true); assert_true(InputManager.gameplay_suppressed(), ...); s.set(&"_is_open", false)` — pins the byte-identical truth set. (3) test_chess_and_chipinstall_are_pausing: ChessScreen._is_open=true -> assert_true(any_pausing_open()) and assert_true(any_modal_open()); reset; same for ChipInstallScreen. (4) test_close_sweep_and_gates_route_through_registry (source-string, mirroring existing test at line 72): assert player.gd `_close_open_modals` source contains `InputManager.close_all_modals()`; assert InputManager.gd `close_all_modals` loops `_modal_screens()` and closes `NameEntryDialog`; assert options_menu.gd and loot_screen.gd each contain `InputManager.any_modal_open(self)`; assert player.gd contains `if InputManager.gameplay_suppressed()` near quicksave and GameState.gd `_load_and_reload` region contains `close_all_modals()`. Use FileAccess.get_file_as_string on res:// paths (same idiom as the existing test). Update the header doc-comment to describe the single _modal_reg source.
- **test:** 
- **docs:** none (test file; the contract doc is updated in F-T1-1).
- **depends_on:** F-T1-1, F-T1-2, F-T1-3, F-T1-4
- **verification:** gut-editor-closed · **effort:** M
- **risk:** GUT 9.x: use assert_eq/assert_true/assert_false/assert_string_contains only (no assert_le/ge). The test flips only `_is_open` flags (no open()/close() side effects, no Player/NPC _ready) — matches the existing file's documented approach (lines 10-11). `s.set(&"_is_open", false)` on every registry screen is safe (all 12 declare the field). Test run itself is editor-closed work (import lock).

### Batch 9 — T2 death/liveness gate: PickupRay, Hotbar, Attack coroutine, die() stash
**Fixes:** F-T2-1, F-T2-2, F-T2-3, F-T2-4  
**Shared god-files:** scripts/player/player.gd, scripts/ui/hotbar.gd, scripts/components/ray_cast.gd  
**Why here:** One theme = one is_alive()/gameplay_suppressed() posture across the four raw-input surfaces; the four fixes touch four different files with no shared static, so they land together as a reviewable liveness commit. F-T2-4 re-touches god-file player.gd (die() region) — distinct from Theme-1's _close_open_modals edit; re-read die() first. No deps.  
**Verify:** NOW: compile via -s; off-tree predicate/helper tests (is_alive default, _fire_should_abort gates, _release_or_stash_carried_prop bag-pulled branch). DEFERRED: input-routing, muzzle-flash-after-await, and the death-mid-carry save-fold are playtest-gated.

#### F-T2-1 · [high] PickupRay: liveness bail + widen E/Z grab gate to gameplay_suppressed (C9, C14, C11-ray)
- **files:** scripts/components/ray_cast.gd
- **change:** In `_unhandled_input(event)` (scripts/components/ray_cast.gd:53) add a liveness bail as the VERY FIRST statement, before the `is_action_pressed("PickUp")` branch:

	var pl := player as Character
	if pl != null and not pl.is_alive():
		return

This single gate covers C9 (a prop grabbed during the keel-over survives the in-place revive still carried — the player node stays in-tree with _dead latched) and C14 (a dead player opening shop/heal/chess via E, which freezes the cinematic). `player` is exported as CharacterBody3D; `player as Character` is a valid downcast (Player extends Character extends CharacterBody3D) and yields null for a bare/AI test body so it simply never bails. is_alive() is `not _dead and hp>0` and is false for the entire death cinematic (Character.take_damage sets _dead=true before _begin_death→die()).

Then close the cutscene/name-entry leak (C11): change the E-grab menu gate at line 58 from `if InputManager.any_modal_open():` to `if InputManager.gameplay_suppressed():`, and the Z-grab gate at line 85 from `if DialogueManager.is_active() or InputManager.any_modal_open():` to `if DialogueManager.is_active() or InputManager.gameplay_suppressed():`. gameplay_suppressed() is a strict SUPERSET of any_modal_open() — same 12 modal screens PLUS CutscenePlayer + NameEntryDialog — so the grabs now also bail during a cutscene and the pet-naming dialog (matching every other combat input), while STILL returning WITHOUT set_input_as_handled() so a transaction screen keeps closing on the same key. Leave the alternate-throw branch (lines 90–106) unchanged (it already gates on gameplay_suppressed). Update the M5 comments at lines 55–58 and 84 to say the gate now spans any menu, cutscenes, and the name-entry dialog (not just modals), and add the liveness-bail intent comment above the new guard.
- **test:** Add tests/test_pickup_ray_liveness.gd (new file; a .gd.uid sidecar generates when the editor is next available). Off-tree per the no-_ready rule: `var r = load("res://scripts/components/ray_cast.gd").new()`. Cannot drive `_unhandled_input` off-tree (needs InputMap + viewport + autoload screens), so assert the PREDICATE seam instead: build a bare Character via `load("res://scripts/player/character.gd").new()`, assert `is_alive()` true by default, then set `c._dead = true` and assert `is_alive()` false — pinning the exact predicate the bail reads. Free with `.free()`. The input-routing itself is playtest-gated. Do NOT assert_le/ge (use assert_true/false).
- **docs:** Nearby code comments in ray_cast.gd (the M5 gate comments at lines 55–58/84 + a new liveness-bail comment). No CURRENT_ARCHITECTURE/AUTHORING entry describes this gate today, so code comments are the contract surface.
- **verification:** compile-check · **effort:** S
- **risk:** ray_cast.gd is the shared carry/interact/talk chokepoint but NOT one of the four god-files; edit is confined to the top of _unhandled_input + two one-token gate swaps. Editor is OPEN: after Write, re-read/grep the file to confirm the edit stuck (buffered .gd can silently revert). Headless `-s` proves compilation only; the input behavior is editor-closed playtest work. gameplay_suppressed()/any_modal_open() are runtime InputManager calls (autoloads exist at runtime) — never called at autoload-init, so no init-order hazard.

#### F-T2-2 · [high] Hotbar: add cutscene + name-entry to the input gate; use is_alive() (C11-hotbar)
- **files:** scripts/ui/hotbar.gd
- **change:** In `_unhandled_input(event)` (scripts/ui/hotbar.gd:134) widen the top guard. Current:

	if _player == null or _player._dead or DialogueManager.is_active() \
			or InputManager.any_modal_open(InventoryScreen):
		return

Change to:

	if _player == null or not _player.is_alive() or DialogueManager.is_active() \
			or CutscenePlayer.is_active() or NameEntryDialog.is_open() \
			or InputManager.any_modal_open(InventoryScreen):
		return

Two edits: (1) swap `_player._dead` → `not _player.is_alive()` for consistency with the class predicate (functionally identical during the cinematic, but public + canonical); (2) add explicit `CutscenePlayer.is_active()` and `NameEntryDialog.is_open()` terms — these are the exactly-two members gameplay_suppressed() adds over any_modal_open(). Hotbar CANNOT call gameplay_suppressed() directly because it must keep `any_modal_open(InventoryScreen)`'s backpack-EXCLUDE (with the bag open, a slot key ASSIGNS the hovered item — handled just below at line 139 — so the backpack must fall through the gate, not be swallowed). Update the block comment at lines 128–133 to state the gate now also covers cutscenes + the name-entry dialog (previously a slot key still swapped weapons / used a medkit during a cutscene and the pet-naming prompt), and note why the two terms are inlined rather than using the gameplay_suppressed superset (the InventoryScreen exclude).
- **test:** Extend tests/test_hotbar.gd: add test_hotbar_gate_respects_liveness(). Off-tree (matches the file's existing pattern): `var p = load(PLAYER_PATH).new()`, `p.inventory = CharacterInventory.new()`, `var hb := Hotbar.new(); hb.setup(p)`. Assert `p.is_alive()` true by default; set `p._dead = true` and assert `not p.is_alive()` — pinning the predicate the gate reads. (The full `_unhandled_input` routing needs the InputMap + viewport and is playtest-gated; the assign/sync/cycle paths already have off-tree coverage.) Free hb/p/inv. Use assert_true/false only (GUT 9.x has no assert_le/ge).
- **docs:** Nearby code comment in hotbar.gd (the gate block at lines 128–133) and the class-header line 19–22 which already says input is gated 'through a conversation, over any menu, or while dead' — extend it to add 'a cutscene, or the name-entry dialog'.
- **verification:** compile-check · **effort:** S
- **risk:** hotbar.gd is UI-only, not a god-file; low blast radius. CutscenePlayer / NameEntryDialog / InputManager are autoloads resolved at runtime (Hotbar._unhandled_input never runs at init), so no autoload-order hazard. Editor OPEN: re-read after Write to confirm the edit persisted. `not _player.is_alive()` is safe because setup() already requires a real _player; the `_player == null` short-circuit still guards the bare case.

#### F-T2-3 · [high] Attack fire coroutine: re-check holster/carry-lock/dialogue/death after each await (C10)
- **files:** scripts/combat/attack.gd
- **change:** Add a post-await abort predicate and call it after BOTH awaits in `_on_mouse_input_attack(_camera, from_ai)`. New helper (place near the fire path, e.g. after line 323):

	## True when a shot queued behind a wind-up / hit-flash await must be ABORTED because the world changed while we
	## waited. Death applies to any wielder (no posthumous muzzle flash / damage); the holster / carry-lock / dialogue
	## gates are player-only (from_ai keeps firing — the world runs in real time for AI). Mirrors the pre-fire guards
	## at the top of this method so a delayed shot honours the same rules a fresh click would.
	func _fire_should_abort(from_ai: bool) -> bool:
		if character != null and not character.is_alive():
			return true
		if from_ai:
			return false
		return holstered or draw_locked or DialogueManager.is_active()

(`character` is typed Character, which has is_alive(); the null-guard covers an off-tree/stub wielder. holstered/draw_locked are Attack's own props read at lines 259–261.)

Wind-up await (lines 315–318): keep the existing weapon-identity guard, then replace the bare tree check:

		if current_weapon != _weapon:
			return
		if not is_inside_tree() or _fire_should_abort(from_ai):
			return

Hit-flash await (lines 332–334): replace `if not is_inside_tree(): return` with a version that also clears the flash it turned on, so an aborted shot never strands the muzzle flash visible:

		if not is_inside_tree() or _fire_should_abort(from_ai):
			_hit_flash.visible = false
			return

Keep `is_inside_tree()` INLINE at both call sites (do not fold it into _fire_should_abort — an off-tree unit test must be able to exercise the holster/lock/from_ai branches without the tree check masking them). Update the two await comments (lines 315–316 and 329–331) to note they now also drop a shot queued behind a holster / carry-lock / dialogue-start / wielder-death, not only a freed wielder.
- **test:** Extend tests/test_attack_draw_lock.gd (same off-tree pattern: `var a = load(ATTACK_PATH).new()`, no add_child, no _ready). Add test_fire_should_abort_gates(): with a fresh Attack, assert `_fire_should_abort(false)` is false by default; set `a.holstered = true` and assert `_fire_should_abort(false)` true; reset, set `a.draw_locked = true` and assert `_fire_should_abort(false)` true; assert `_fire_should_abort(true)` false in BOTH holstered and draw_locked states (an AI wielder ignores the player-only gates). `character` is null off-tree so the death branch is skipped; DialogueManager autoload returns false under GUT. Free with `.free()`. assert_true/false only.
- **docs:** Nearby code comments in attack.gd (the new helper docstring + the two await-guard comments). The fire path is not documented in CURRENT_ARCHITECTURE beyond code.
- **verification:** compile-check · **effort:** M
- **risk:** attack.gd is the BYTE-ORDER-CRITICAL fire hot path — but _fire_should_abort runs at most twice per shot (post-await only), never per-pellet, so zero cadence cost. Do NOT reorder the pre-fire guards or the per-pellet loop. The hit-flash abort now writes `_hit_flash.visible=false`; `_hit_flash` was already dereferenced on the success path just below, so it is non-null whenever this branch is reached (guarded by the enclosing `if _hit_flash and ...` at line 326). Editor OPEN: re-read after Write. Headless `-s` proves compilation; muzzle-flash/timing behavior is playtest-gated.

#### F-T2-4 · [high] die(): stash a bag-pulled prop back to the backpack instead of dropping it (C18)
- **files:** scripts/player/player.gd
- **change:** In `die()` (scripts/player/player.gd:2144) the current `_force_release_carried_prop()` drops WHATEVER is carried into the world. If the carried prop was PULLED FROM THE BACKPACK via the hotbar (`_held_inv_item != null`), force-release routes through _on_carry_changed(false) which clears the reservation, and the death-milestone autosave (the wallet-bequeath money_changed at Character.take_damage → _queue_autosave, flushed one frame later) then persists a profile with the item neither in-bag nor in the non-snapshot world → quitting before re-collecting loses it. Extract the decision into a new private method and call it in die():

Replace the `_force_release_carried_prop()` line in die() with `_release_or_stash_carried_prop()`.

Add:

	## Death carry-teardown: a prop PULLED FROM THE BAG (hotbar hold, _held_inv_item set) is STASHED back into the
	## backpack — not dropped — so the death-milestone autosave (wallet bequeath / respawn) persists the item WITH the
	## profile. Dropping it clears the reservation and the item lives only as a non-persisted world prop → lost on the
	## next load. stash_held_item() re-adds the SAME instance, then releases + frees the world copy; if the bag is full
	## it REFUSES (keeps holding), and held_inventory_item() staying non-null makes the save-fold capture it instead —
	## either way the item survives. A plain aimed grab (no _held_inv_item) still force-releases into the world as before.
	func _release_or_stash_carried_prop() -> void:
		if _held_inv_item != null:
			stash_held_item()
		else:
			_force_release_carried_prop()

Calling stash_held_item() here is safe: it runs BEFORE set_physics_process(false)/HUD-hide in die(), inventory is valid, and its inventory.add fires the changed→_queue_autosave that folds the item into the save. Its internal force_release (only when head.pickup_ray.held_object is set) emits carry_changed(false), but stash clears _held_inv_item first so _on_carry_changed treats it as a put-back (no stale reservation).
- **test:** Extend tests/test_hotbar.gd (already has PLAYER_PATH + CharacterInventory off-tree helpers and a _make_holdable). Add test_death_teardown_stashes_a_bag_pulled_prop(): `var p = load(PLAYER_PATH).new()`, `var inv := CharacterInventory.new(); p.inventory = inv`, `var dog := _make_holdable(&"dog")` — simulate a live hold with the item OUT of the bag and reserved (`p._held_inv_item = dog`, do NOT inv.add). Call `p._release_or_stash_carried_prop()`; assert `inv.has(dog)` true and `p.held_inventory_item() == null` (the bag-pulled branch stashed it back). Then a second case: `p._held_inv_item = null` and assert `p._release_or_stash_carried_prop()` is a safe no-op (no head/pickup_ray off-tree — _force_release_carried_prop null-guards on head). This exercises the new branch WITHOUT running die() (which cannot run off-tree — it drives the full cinematic + child nodes). Free p/inv. assert_true/false/eq/null only.
- **docs:** Nearby code comment in player.gd (the new method docstring + a one-line note at the die() call site). docs/CURRENT_ARCHITECTURE.md save-model section: if it enumerates what a checkpoint/profile save folds in (held-in-hand item), add a line that a death mid-carry of a bag-pulled prop stashes it so the milestone autosave keeps it — otherwise code comment suffices (mark docs impact: minimal).
- **verification:** code-only · **effort:** S
- **risk:** player.gd is a GOD-FILE also edited by Theme 1 (death/modal-close) and Theme 4 (save fidelity) — collision risk on die(). Keep this edit to the single call-site swap + one new method appended near stash_held_item()/_force_release_carried_prop() (lines ~1560–1596); do NOT touch other die() lines Theme 1 owns. Editor OPEN: re-read after Write to confirm persistence. stash_held_item may emit a 'No room' toast on a full bag mid-death (HUD is about to hide) — cosmetically harmless edge; the save-fold still preserves the item. Runtime death path is playtest-gated (die() can't run off-tree); the extracted method is unit-tested.

### Batch 10 — T3 zorkmids chokepoint: is_mirrored + atomic transfer_to, loot debit, is_holdable exclude
**Fixes:** F-C12, F-C37, F-C13-C28  
**Shared god-files:** scripts/inventory/character_inventory.gd  
**Why here:** F-C12 is the chokepoint (adds CharacterInventory.is_mirrored/register_mirror + defers transfer_to's own `changed` across remove+rollback) and must precede F-C37 (loot debit now gated on is_mirrored). F-C13-C28 (Item.is_holdable excludes Zorkmids) is independent but same theme. Re-touches god-file character_inventory.gd — re-read after Batch 4's _rehome edit.  
**Verify:** NOW: compile via -s; off-tree tests (rollback survives a listener refit, live-pickpocket coin tile doesn't debit the float, coin tile not holdable). GUT run deferred editor-closed.

#### F-C12 · [high] transfer_to atomic rollback: defer own `changed` + check add() return; add is_mirrored predicate MoneyPurse registers
- **files:** scripts/inventory/character_inventory.gd, scripts/inventory/money_purse.gd
- **change:** In character_inventory.gd add two private fields near line 45: `var _defer_changed: bool = false` and `var _pending_changed: bool = false`, plus a helper `func _emit_changed() -> void:` that, when `_defer_changed`, sets `_pending_changed = true` and returns, else `changed.emit()`. Replace the `changed.emit()` at the end of add() (line 130) and remove() (line 195) with `_emit_changed()` (leave equipped_item_lost.emit() immediate; leave set_item_count/remove_stack/move_stack untouched — transfer_to never calls them). Rewrite transfer_to (line 402): after the guards and `var was_equipped := (item == equipped_item)`, wrap the source-side mutation so intermediate `changed` on THIS inventory is coalesced across remove + rollback: set `_defer_changed = true`; `var removed := remove(item, move)`; `var moved := other.add(item, removed)` (other's own emissions stay independent); `if moved < removed:` then `var back := add(item, removed - moved)` — capture the return; `if back < (removed - moved): push_warning("CharacterInventory.transfer_to: rollback re-homed only %d/%d of '%s' — restoring remainder unplaced" % [back, removed - moved, item.label()]); restore_stack(item, (removed - moved) - back)` so the remainder is kept (unplaced-tolerant) rather than destroyed; then the existing was_equipped/equip_weapon_requested restore block; finally `_defer_changed = false` and `if _pending_changed: _pending_changed = false; changed.emit()` (emit exactly once, AFTER the rollback has settled so a mirror listener sees only the final state). Also add the mirror-registry seam: `var _mirrored_ids: Dictionary = {}` near line 45; `func register_mirror(id: StringName) -> void:` that stores non-empty ids as keys; `func is_mirrored(item: Item) -> bool: return item != null and _mirrored_ids.has(item.id)` — documented as 'true when this item is a DERIVED VIEW (a MoneyPurse coin tile), not real loot'. In money_purse.gd _ready() (after the inventory.changed connect, ~line 36) call `_host.inventory.register_mirror(Zorkmids.ITEM_ID)` so only the player's backpack reports the coin tile as mirrored (NPC/corpse/container bags have no purse → never register).
- **test:** Add to tests/test_character_inventory_grid.gd: func test_transfer_to_rollback_survives_listener_refit() — src.enable_grid(1,1) holding item `a`(1x1); dst.enable_grid(1,1) filled (bounces `a` back); connect src.changed to a bound method on a tiny inner helper class (NOT a lambda) that, guarded by `if src.count_of(a)==0 and src.count_of(coin)==0`, calls src.set_item_count(coin,1,1,1) to grab the freed cell (models MoneyPurse.sync). assert src.transfer_to(dst,a,1)==0 and assert_eq(src.count_of(a),1, 'bounced item restored, not eaten by mid-transfer refit') and assert_eq(src.count_of(coin),0). This FAILS today (listener fires on remove()'s changed, eats the cell) and PASSES after the deferral. Also add tests/test_money_purse.gd: func test_registers_zorkmids_mirror_on_ready() building a bare Character-like host inventory and asserting is_mirrored(zorkmids item) — build off-tree per the no-_ready rule (call inventory.register_mirror(&"zorkmids") directly, or add_child the purse with _host set and assert inventory.is_mirrored). Existing transfer_to tests (test_character_inventory.gd:301-325, grid:93-137) assert counts not signal-emit-count, so the coalesced single emit doesn't break them.
- **docs:** docs/CURRENT_ARCHITECTURE.md money/inventory paragraph (~line 59-67): add one sentence that transfer_to coalesces its own `changed` across remove+rollback and checks add()'s return so a mirror listener (MoneyPurse) can never consume the freed cells mid-rollback; note CharacterInventory.is_mirrored(item) is the one-place predicate MoneyPurse registers. Update the transfer_to and register_mirror/is_mirrored doc-comments in character_inventory.gd to state the invariant.
- **verification:** gut-editor-closed · **effort:** M
- **risk:** character_inventory.gd is a shared inventory god-file touched by loot/shop/merchant/npc_scavenge — the deferral only changes WHEN `this` emits `changed` during transfer_to (now once at the end instead of twice), never the resulting counts/placements, so those callers are unaffected; but any future test asserting per-transfer emit count would see 1. Not byte-order/hot-path. money_purse.gd register call runs once at _ready (player only).

#### F-C37 · [high] Looting a zorkmids tile must not also debit the source's separate `money` float (destroys NPC loot)
- **files:** scripts/ui/loot_screen.gd
- **change:** In _take()'s zorkmids branch (line 263-275), gate the money-float debit on the source actually MIRRORING the tile. Change the `if _money_source != null and is_instance_valid(_money_source):` at line 269 to `if _money_source != null and is_instance_valid(_money_source) and _source_inv.is_mirrored(item):`. Rewrite the nearby comment (line 261-262): the debit is only correct when the source's coin tile is a genuine MIRROR of a wallet float (a MoneyPurse owner). Corpses, containers, and live-pickpocket NPCs have NO purse — their coin tile is REAL loot, and debiting their separate `money` float would destroy that cash (player gets the tile, NPC loses tile AND float). is_mirrored(item) (added in F-C12) is the single predicate; since a loot source is never the player, this now correctly never double-debits.
- **test:** Add tests/test_loot_drop.gd: func test_take_zorkmids_tile_from_live_pickpocket_does_not_debit_float() — reuse the existing _PickpocketTarget helper + `load('res://scripts/player/player.gd').new()` pattern (no _ready). Give mark.inventory a zorkmids tile (set_item_count(ZORKMIDS_ITEM, 500)) AND mark.money = 3.0; LootScreen.pickpocket(mark, player); LootScreen._take(ZORKMIDS_ITEM). Assert player.money increased by 5.0 (500 * QUANTUM), assert_eq(mark.money, 3.0, 'the separate pocket float is NOT debited — it is not a mirror') and assert_eq(mark.inventory.count_of(ZORKMIDS_ITEM), 0). This calls only the non-UI branch of _take (remove + add_money + _maybe_free_drained_corpse), matching how test at line 276 drives _deposit directly.
- **docs:** docs/CURRENT_ARCHITECTURE.md (~line 69-78 loot-cash paragraph): add that a loot source's coin tile is REAL loot (its `money` float is separate pocket cash taken via the FLOAT button) and is only ever mirror-debited when CharacterInventory.is_mirrored(item) is true — never for corpses/containers/pickpocket NPCs.
- **depends_on:** F-C12
- **verification:** gut-editor-closed · **effort:** S
- **risk:** loot_screen.gd is not a god-file but is shared by corpse/container/pickpocket/exchange flows; the change only narrows an existing debit branch, so TILE-mode corpses/containers (already _money_source==null) are unchanged. Requires F-C12's is_mirrored to exist or this fails to compile.

#### F-C13-C28 · [high] The zorkmids coin tile must not be hotbar-assignable/holdable (minting a money-bag prop from the wallet)
- **files:** scripts/items/item.gd
- **change:** In item.gd is_holdable() (line 135), add a guard as the FIRST line: `if id == Zorkmids.ITEM_ID:` return false — with a comment: the wallet coin tile wears a `world_model` (the bag mesh) but is MONEY, not a carryable prop; slotting it on the hotbar (hotbar.gd:207 assign) then activating it (hotbar.gd:242 _activate) would pull a physics money-bag prop into the hands out of nothing while MoneyPurse instantly re-mints the debited wallet. Fixing the ONE chokepoint (is_holdable) covers both the assign guard and the _activate holdable branch, since both funnel through is_holdable(). Zorkmids is a RefCounted class_name with `const ITEM_ID` and does not reference Item, so there is no parse cycle. Leave the existing weapon/consumable/ammo/upgrade_chip guard below it unchanged.
- **test:** Add tests/test_hotbar.gd (where the is_holdable classification tests live, ~line 137): func test_zorkmids_coin_tile_is_not_holdable() — build `var z := Item.new(); z.id = &'zorkmids'; z.world_prop = 'res://x.tscn'` (or set a dummy world_model); assert_false(z.is_holdable(), 'the wallet coin tile is money, never a holdable prop even with a world_model'). Optionally assert a non-zorkmids world_model item still holds (guards against an over-broad guard). Item resources need no _ready, so `.new()` is fine.
- **docs:** Add a short note in the is_holdable() doc-comment (item.gd:131-134) that the Zorkmids coin tile is explicitly excluded despite carrying a world_model. docs/AUTHORING_GUIDE.md holdable-props/hotbar section (if it enumerates what can be slotted): note the wallet tile is excluded.
- **verification:** compile-check · **effort:** S
- **risk:** item.gd is a widely-referenced Resource base but is_holdable is only consumed by hotbar.gd assign/_activate; grid_tile/inventory tile rendering keys off world_model/icon, NOT is_holdable, so the coin tile still renders its bag mesh. No hot path, no god-file. Low risk.

### Batch 11 — T4 save-fidelity: gone-bit as_bool, pickup persistence, name/stat migration, hotbar doc
**Fixes:** F-C45, F-C16, F-C17, F-C43, F-C44, F-C42  
**Shared god-files:** managers/GameState.gd, scripts/ui/hotbar.gd  
**Why here:** Ordered per theme note: F-C45 (as_bool reads) first so F-C16 (MoneyPickUp/UpgradePickup persistence, depends C45) copies the corrected read verbatim. GameState.gd hunks land as one serialized pass: F-C17 (_cfg_str name guard) → F-C43 (SAVE_VERSION=2, persuasion→streetwise, depends C17) → F-C44 (clear _load_warnings) — distinct hunks, re-read before each (this is GameState.gd's 3rd batch). F-C42 corrects the false hotbar-persistence comment. New preload const in C16 needs a .gd.uid (editor-closed).  
**Verify:** NOW: compile via -s; off-tree GameState round-trip tests (gone String→false, name junk-guard, v1 persuasion folds to streetwise, no double-count). DEFERRED: pickup _ready 'gone' free path is playtest-gated; new-script sidecars in the editor-closed pass.

#### F-C45 · [medium] Read the world-object "gone" bit through GameState.as_bool (match Door)
- **files:** scripts/components/can_pick_up.gd, scripts/components/can_destroy.gd
- **change:** In can_pick_up.gd:55 wrap the ledger read: change `if not build_model_from_item and GameState.object_state(GameState.current_level_path, _save_key()).get("gone", false):` to `if not build_model_from_item and GameState.as_bool(GameState.object_state(GameState.current_level_path, _save_key()).get("gone", false)):`. In can_destroy.gd:31 change `if not Engine.is_editor_hint() and GameState.object_state(GameState.current_level_path, _save_key()).get("gone", false):` to `if not Engine.is_editor_hint() and GameState.as_bool(GameState.object_state(GameState.current_level_path, _save_key()).get("gone", false)):`. This matches door.gd:70-72, which already reads open/locked through GameState.as_bool — a hand-edited/legacy gamestate.cfg can hold a String under the gone key, and bare truthiness on a non-empty String reads true (or bool() would crash if fed directly elsewhere); as_bool coerces numerics and degrades non-numeric junk to the default. Update the one-line comment above each read to note the as_bool guard (mirrors Door).
- **test:** Add to tests/test_game_save.gd a pure GameState test (fresh instance via load(GAMESTATE_PATH).new(), NO node _ready): record_object_state(lvl, key, {"gone": "true"}) with a STRING value, then assert as_bool(object_state(lvl,key).get("gone", false)) is false (a String is not a valid bool -> fallback), and record {"gone": true} asserts true. Reuses the existing as_bool coverage pattern at test_game_save.gd:505-512. Do NOT add_child a CanPickUp/CanDestroy (its _ready hits the real GameState autoload and builds meshes).
- **docs:** Update the one-line intent comment above each guarded read in can_pick_up.gd / can_destroy.gd to say the gone bit is coerced via GameState.as_bool (persisted-Variant safety), consistent with Door.
- **verification:** compile-check · **effort:** S
- **risk:** Behaviour-preserving for well-typed saves (bool true still reads true). Both are @tool-adjacent components but the read is already runtime-gated. No hot path. No shared god-file.

#### F-C16 · [critical] MoneyPickUp/UpgradePickup: record + honor the world_objects "gone" bit (stop respawn/infinite-money)
- **files:** scripts/components/money_pickup.gd, scripts/components/upgrade_pickup.gd, scripts/components/money_bag.gd
- **change:** Mirror CanPickUp's persistence exactly on both pickups. (1) Add at top of each: `const WorldSaveId = preload("res://scripts/world/world_save_id.gd")`. (2) Add exports: `@export var save_id: StringName = &""` (stable per-object key; blank = level+path+position fallback) and `@export var persist_collected: bool = true` (a hand-placed pickup persists "gone"; a code-spawned one opts out — see money_bag). (3) Add `func _save_key() -> String: return WorldSaveId.key_for(self, save_id)`. (4) In each _ready, BEFORE building the visual/super(), add the collected-check: for money_pickup.gd (not @tool) `if persist_collected and GameState.as_bool(GameState.object_state(GameState.current_level_path, _save_key()).get("gone", false)): queue_free(); return`; for upgrade_pickup.gd (@tool) place it after the existing `if Engine.is_editor_hint(): return` guard, same body. (5) In each start_talk, AFTER the grant commits and BEFORE the deferred host free, add `if persist_collected and not Engine.is_editor_hint(): GameState.record_object_state(GameState.current_level_path, _save_key(), {"gone": true})` (money_pickup: guard is just `if persist_collected:` since it is not @tool, but keep the editor guard harmless-safe; use the same line as upgrade for symmetry). (6) In money_bag.gd, set `reclaim.persist_collected = false` on the code-built MoneyPickUp BEFORE `bag.add_child(reclaim)` at line ~74 — a dropped money bag is a dynamic spawn with no stable identity and must never enter the ledger (would clutter it and could mis-key). Use the exact as_bool read shape from F-C45.
- **test:** tests/test_pickups.gd (off-tree, no _ready): (a) assert MoneyPickUp.new().persist_collected and UpgradePickup.new().persist_collected default true, and both expose a `save_id` StringName property (property-surface check like test_canpickup_does_not_auto_body_authored_pickups at test_pickups.gd:71); (b) money_bag wiring test: call MoneyBag.build(...) (or instance the reclaim path) and assert the child MoneyPickUp has persist_collected==false — pins the opt-out so a dropped bag never persists. The gone round-trip mechanism itself is already covered by test_game_save.gd:105-123; the in-tree _ready free is playtest-gated (reads the real GameState autoload).
- **docs:** GameState.gd header (lines 18-23) and CLAUDE.md "Save semantics" both enumerate the ledger as "consumed CanPickUp / destroyed CanDestroy" — extend to "...consumed CanPickUp / MoneyPickUp / UpgradePickup / destroyed CanDestroy". docs/CURRENT_ARCHITECTURE.md save model: same addition. docs/AUTHORING_GUIDE.md: note the new `save_id` on money/upgrade pickups (author it for hand-placed pickups whose collected-state must survive layout edits, mirroring CanPickUp/CanDestroy) and that `persist_collected=false` is for code-spawned pickups only.
- **depends_on:** F-C45
- **verification:** playtest-gated · **effort:** M
- **risk:** UpgradePickup is @tool — the _ready collected-check MUST sit after the Engine.is_editor_hint() early-return or it touches GameState in-editor (forbidden). money_pickup.gd is NOT @tool. Set persist_collected=false in money_bag BEFORE add_child or the reclaim's _ready records a stray key. Fallback WorldSaveId key uses global_position (valid in _ready/in-tree only) — fine here. No shared god-file; no hot path. New preload const needs the editor to regenerate .gd.uid when next available (compilation unaffected).

#### F-C17 · [high] Guard the unguarded String() cast on the persisted player name (abort-on-junk load)
- **files:** managers/GameState.gd
- **change:** Add a `_cfg_str` helper to the type-guarded read block (after _cfg_bool, ~line 274): `static func _cfg_str(cfg: ConfigFile, section: String, key: String, fallback: String) -> String:` returning `var v = cfg.get_value(section, key, fallback); return v if v is String else fallback`. Then replace line 190 `player_name = String(cfg.get_value("player", "name", ""))` with `player_name = _cfg_str(cfg, "player", "name", "")`. String(<Variant>) can raise "Invalid constructor" on some junk-typed values, which aborts load_from_disk before `loaded = true`, and the first autosave then overwrites the save with a fresh profile — the exact abort chain the file's own _cfg_* contract (lines 261-278) exists to prevent. name is always written as a String (save_to_disk line 289), so `v is String else ""` is lossless.
- **test:** 
- **docs:** Extend the _cfg_* block comment (lines 261-263) to mention _cfg_str. Update the inline comment on the (now guarded) name read to note junk/missing -> unnamed via the type guard (drop the raw-cast rationale).
- **verification:** compile-check · **effort:** S
- **risk:** Shared god-file GameState.gd (C43/C44 also edit it) — apply as a distinct hunk (line 190 + helper block) and re-read before editing (open-editor buffered-revert risk per project note). Boot-path code: a mistake here bricks load; keep the guard type-exact. Not a hot path.

#### F-C43 · [high] Stat-overhaul migration: fold legacy `persuasion` into `streetwise`, bump SAVE_VERSION to 2
- **files:** managers/GameState.gd
- **change:** Change `const SAVE_VERSION := 1` (line 33) to `const SAVE_VERSION := 2`. After the stat-load loop (lines 210-212, which skips any key not in STAT_NAMES and so silently drops `persuasion`), add: `# C43: pre-2026-07-09 saves stored a "persuasion" stat later renamed to "streetwise"; the loop above ignores unknown keys, dropping the points. Fold the legacy value into streetwise for any save older than v2 so the build survives the rename. if save_version < 2 and cfg.has_section("stats") and cfg.has_section_key("stats", "persuasion"): stat_values[&"streetwise"] = int(stat_values.get(&"streetwise", 0)) + _cfg_int(cfg, "stats", "persuasion", 0)`. save_version is read at line 188 before this, so the branch is available. A current v1 save has no persuasion key -> has_section_key false -> no-op; a fresh game already sets save_version = SAVE_VERSION (reset_for_new_game line 653) so it is v2. Re-saving migrates the file to v2 and the branch never re-runs.
- **test:** tests/test_game_save.gd (fresh GameState instance, no _ready): write a ConfigFile with [meta].version=1, [stats].persuasion=5, [stats].streetwise=2, save to TMP; load_from_disk; assert make_stats().get_stat(&"streetwise") (or stat_values[&"streetwise"]) == 7. Second case: a v2 save with streetwise=3 and NO persuasion key loads streetwise==3 unchanged (no double-count). Follows the round-trip pattern at test_game_save.gd:105-123.
- **docs:** Update the SAVE_VERSION comment block (lines 29-32) to record that v2 = the stat-overhaul rename migration (persuasion->streetwise) and that this is the first version-gated read. Note the migration inline (comment above the branch). Mention in docs/CURRENT_ARCHITECTURE.md save model that legacy persuasion points migrate to streetwise on load.
- **depends_on:** F-C17
- **verification:** compile-check · **effort:** M
- **risk:** Shared god-file GameState.gd — distinct hunk from C17/C44; re-read before edit. SAVE_VERSION is a one-line const only in this file (no cross-theme collision), but the bump means every save written after this is stamped v2 — intended. Verify the old stat key is literally "persuasion" (confirmed by review C43 and the absence of persuasion from STAT_NAMES line 36); if a different legacy name surfaces in an old cfg, add it to the same branch. Not a hot path.

#### F-C44 · [medium] reset_for_new_game must clear _load_warnings (stale toast on a fresh game)
- **files:** managers/GameState.gd
- **change:** In reset_for_new_game (line 651), add `_load_warnings.clear()` alongside the other ledger/tracker clears (e.g. next to `_quests_active.clear()` ~line 671). A failed boot-load populates _load_warnings (GameState.gd:618/628/638); the HUD consumes them once via take_load_warnings on _ready. Without clearing on New Game, starting a brand-new run after a boot whose saved quests failed to resolve toasts those stale warnings on the fresh game. take_load_warnings duplicates+clears, but ui.gd may not have consumed them before New Game is chosen, so clear them explicitly here.
- **test:** 
- **docs:** Add a trailing comment on the new line: `# C44: forget any prior boot-load's quest-restore warnings so a fresh game doesn't toast them`. No external doc surface.
- **verification:** compile-check · **effort:** S
- **risk:** Shared god-file GameState.gd — distinct hunk from C17/C43; re-read before edit. Purely additive clear; no behavior change for an existing run. Not a hot path.

#### F-C42 · [medium] Hotbar: correct the false "saved game rebuilds the same layout" claim
- **files:** scripts/ui/hotbar.gd
- **change:** The class comment (lines 17-18) claims "Slot assignment is DERIVED from bag contents in insertion order, so a saved game rebuilds the same layout." That is true ONLY for AUTO-filled weapons/consumables; MANUALLY-assigned holdable props (dog/crate via assign()) and any custom rearrangement (assign() swaps) are session-only and NOT serialized, so they revert on reload. Rewrite the sentence to: "AUTO-filled weapon/consumable slots are DERIVED from bag contents in insertion order, so a reload rebuilds that ordering identically. MANUAL holdable-prop assignments and custom slot swaps (assign()) are a session-only convenience — they are NOT persisted, so the bar re-derives from bag order on reload (the props themselves survive in the backpack, which IS saved)." This adopts the review's sanctioned "correct the doc claim" option — full [hotbar] serialization is disproportionate here (the Hotbar is runtime-built by ui.gd over live Item instances, holdable-prop layout is a UX nicety, and adding a capture/restore section would touch the GameState god-file + capture/restore ordering during heavy scene authoring).
- **test:** 
- **docs:** The edited hotbar.gd class comment IS the doc. If docs/CURRENT_ARCHITECTURE.md or AUTHORING_GUIDE mentions hotbar-layout persistence, align it to "auto-fill order persists; manual assignment is session-only" (grep hotbar in docs first).
- **verification:** code-only · **effort:** S
- **risk:** Comment-only; zero runtime change. If the user later wants true layout persistence, that is a separate M-effort feature (GameState [hotbar] section keyed by item.id, restored after inventory rebuild in ui.gd setup) — deferred, not in this fix. No god-file touched.

### Batch 12 — T5 chess + chip money seams: wager settlement, install pre-charge guard, contracts
**Fixes:** F-T5-1, F-T5-2, F-T5-3  
**Shared god-files:** scripts/player/player.gd  
**Why here:** The two untested money seams. F-T5-1 adds a _player_moved flag + pure static settle helpers to chess_screen.gd (fixes opponent-White free-forfeit) and F-T5-2 adds Player.can_grant_mechanic gating both chip-install charge paths + the ABILITY_SCRIPTS↔AbilityRegistry drift test. F-T5-3 extends the duck-type contract test to all 5 ChessMatch methods + chip open. Independent files; F-T5-2 re-touches player.gd (adds a self-contained method after unlock_mechanic — re-read).  
**Verify:** NOW: compile via -s; off-tree static-helper unit tests (forfeit/decided deltas), off-tree install-refused-no-charge tests, ABILITY_SCRIPTS drift test. DEFERRED: live wager/install money flows are playtest; new test_chess_wager.gd sidecar editor-closed.

#### F-T5-1 · [high] Chess wager: fix free-forfeit vs opponent-White + extract & test pure settlement (C20/C27/C25)
- **files:** scripts/ui/chess_screen.gd, tests/test_chess_wager.gd, scripts/chess/README.md
- **change:** Root cause: close() forfeits on `not _san_log.is_empty()`, but when the opponent is White the AI auto-plays move 1 into _san_log before the player ever moves (open_match L118 `_begin_ai_turn()`), so opening + Esc costs the full wager. Fix by tracking a PLAYER-move flag and routing all wager math through pure static helpers.

1) Add state flag: after `var _finished := false` (L49) add `var _player_moved := false  ## true once the PLAYER has made >=1 move — the ONLY thing that arms a forfeit (an AI opening move must not).`.
2) Reset it on open: in open_match beside `_finished = false` (L101) add `_player_moved = false`.
3) Set it on a real player move: in _submit_move, immediately after `_apply_and_log(m)` (L172) add `_player_moved = true`.
4) Add a pure settlement section (place after _end_game, ~L236). Static so it is testable off-tree without the CanvasLayer:
```
# --- Wager settlement (pure; unit-tested in tests/test_chess_wager.gd) ---
## +1 player win / -1 player loss / 0 draw, from a DECIDED game's result and the player's colour.
static func player_result_sign(result: int, player_color: int) -> int:
	if (result == ChessGame.Result.WHITE_WINS and player_color == ChessGame.WHITE) \
			or (result == ChessGame.Result.BLACK_WINS and player_color == ChessGame.BLACK):
		return 1
	if (result == ChessGame.Result.WHITE_WINS and player_color == ChessGame.BLACK) \
			or (result == ChessGame.Result.BLACK_WINS and player_color == ChessGame.WHITE):
		return -1
	return 0
## Signed money delta when a DECIDED game settles (called ONCE from _end_game): win +wager, loss -wager, draw 0.
static func decided_delta(result: int, player_color: int, wager: int) -> int:
	if wager <= 0:
		return 0
	return player_result_sign(result, player_color) * wager
## Signed money delta when a game is ABANDONED before it is decided. Forfeit (-wager) ONLY if the player has
## actually moved; leaving before your first move — incl. an opponent-White table you never replied to — is free.
static func forfeit_delta(wager: int, player_moved: bool) -> int:
	if wager <= 0 or not player_moved:
		return 0
	return -wager
```
5) Rewrite the close() forfeit block (current L126-132) to delegate:
```
	# Abandoning a wagered game the player has already MOVED in (but that isn't decided) forfeits the stake, so a
	# player can't bail every losing position for free. Friendly (wager 0), not-yet-moved, or already-settled = free.
	if not _finished:
		var delta := forfeit_delta(_wager, _player_moved)
		if delta != 0 and is_instance_valid(_player):
			_player.add_money(float(delta))
			if _player.has_method(&"notify_toast"):
				_player.notify_toast("[PH] -%s zm — you forfeited the game." % Zorkmids.fmt(float(-delta)), MenuStyle.danger())
```
6) Rewrite the _end_game payout block (current L222-234) to use decided_delta, dropping the inline player_won/player_lost locals:
```
	var res := _game.status()
	var delta := decided_delta(res, _player_color, _wager)
	if delta != 0 and is_instance_valid(_player):
		_player.add_money(float(delta))
		if _player.has_method(&"notify_toast"):
			if delta > 0:
				_player.notify_toast("[PH] +%s zm — you won the game." % Zorkmids.fmt(float(delta)), MenuStyle.gold())
			else:
				_player.notify_toast("[PH] -%s zm — you lost the game." % Zorkmids.fmt(float(-delta)), MenuStyle.danger())
```
(_status_text still classifies win/lose independently via _game.status(); it stays consistent with player_result_sign.) No hardcoded gameplay numbers introduced — wager stays the authored ChessMatch export.
- **test:** NEW tests/test_chess_wager.gd (`extends GutTest`). `const CS := preload("res://scripts/ui/chess_screen.gd")` — call the STATIC helpers directly, no instantiation, no _ready, headless-safe. Assertions (use assert_eq): forfeit_delta(0,true)==0 (friendly free); forfeit_delta(100,false)==0 — the C20 regression: opponent-White table left before replying is FREE; forfeit_delta(100,true)==-100 (moved then bailed); decided_delta(ChessGame.Result.WHITE_WINS, ChessGame.WHITE, 100)==100 (win); decided_delta(ChessGame.Result.WHITE_WINS, ChessGame.BLACK, 100)==-100 (loss as Black); decided_delta(ChessGame.Result.STALEMATE, ChessGame.WHITE, 100)==0 (draw even); decided_delta(ChessGame.Result.WHITE_WINS, ChessGame.WHITE, 0)==0 (friendly). ChessGame is a global class_name so its enum is reachable off-tree. GUT 9.x: assert_eq only (no assert_le/ge here).
- **docs:** scripts/chess/README.md L36-37: append that a wager is forfeited only once the PLAYER has moved — opening a table (incl. an opponent-White one whose AI has already moved) and leaving before your first move costs nothing. Update the close() comment as shown. docs impact: AUTHORING_GUIDE L1987 already states win/lose/draw and needs no change.
- **verification:** compile-check · **effort:** M
- **risk:** chess_screen.gd is single-theme (not a god-file) — low collision, but it may already be among the 314 dirty files; touch only these blocks and re-read after editing (open-editor buffers can revert a Write). The forfeit/payout is money-mutating: the `not _finished` guard in close() plus the single decided_delta call in _end_game must both remain so a decided game is never double-settled. New test file's .gd.uid sidecar can't be generated now (editor open / import lock) — GUT still discovers *.gd; generate the uid on the next editor import.

#### F-T5-2 · [high] ChipInstaller: verify the grant resolves BEFORE charging + ABILITY_SCRIPTS↔AbilityRegistry drift test (C21)
- **files:** scripts/components/chip_installer.gd, scripts/player/player.gd, tests/test_chip_install.gd, tests/test_upgrades.gd
- **change:** Root cause: install_carried (L149) / buy_and_install (L165) charge, consume the chip, then _grant -> unlock_mechanic -> _make_ability, which returns null for an id absent from Player.ABILITY_SCRIPTS (a typo'd installs_ability). Money is gone, chip consumed, autosave + "installed" toast fire, ability granted = nothing.

1) Add a resolvability query on Player, placed right after unlock_mechanic (~L979) in player.gd:
```
## True iff unlock_mechanic(id) would actually grant something: either an ability node with this id already
## exists to re-enable, or the runtime registry can build one. Lets a PAID install (ChipInstaller) verify the
## grant resolves BEFORE charging, so a typo'd chip id never takes money for nothing.
func can_grant_mechanic(id: StringName) -> bool:
	if ABILITY_SCRIPTS.has(id):
		return true
	for a in _abilities:
		if a != null and a.ability_id() == id:
			return true
	return false
```
2) In chip_installer.gd install_carried, insert AFTER the has_mechanic/not-held guard (after L153) and BEFORE `var cost := install_fee(item)`:
```
	if not player.can_grant_mechanic(item.installs_ability):
		push_warning("ChipInstaller: chip '%s' installs unknown ability '%s' — install refused, no charge." % [item.label(), item.installs_ability])
		return false
```
3) Same guard in buy_and_install, AFTER the has_mechanic/not-stocked guard (after L169) and BEFORE `var cost := buy_and_install_cost(item)`. `player` is already cast to Player in both funcs (early null-return), so the typed call is safe.
4) Update the _grant comment (L179-180) / class ECONOMY comment to note the charge is gated on can_grant_mechanic so a null grant can't be paid for.
- **test:** tests/test_chip_install.gd: add test_install_refused_when_ability_unresolvable() — build `_installer()` + `_player(1000.0)`, a `_chip(&"nonexistent_ability", 400.0)` added to the bag, assert_false(m.install_carried(chip,p)), assert_eq(p.money,1000.0) (no charge), assert_true(p.inventory.has(chip)) (chip kept), assert_false(p.has_mechanic(&"nonexistent_ability")). Mirror one for buy_and_install with the chip in m.stock. Both off-tree (no _ready), matching the file's existing pattern. tests/test_upgrades.gd: add the DRIFT test test_ability_scripts_covers_registry_ids() — `var scripts: Dictionary = load("res://scripts/player/player.gd").ABILITY_SCRIPTS` (const read, no instantiate); `for id in AbilityRegistry.ids(): assert_true(scripts.has(StringName(id)), "AbilityRegistry id '%s' (editor dropdown, from scenes/) must map in Player.ABILITY_SCRIPTS or a fresh install/save-load can't build it" % id)`. Currently all 7 ids resolve, so the test is green and pins the two registries together. GUT 9.x assert_true/eq only.
- **docs:** scripts/chess/README.md L44-46 already ties ChessVisualizer into ABILITY_SCRIPTS — extend that bullet (or the chip-authoring bullet L47) to name the new drift test tests/test_upgrades.gd::test_ability_scripts_covers_registry_ids and the pre-charge can_grant_mechanic guard. Update the chip_installer.gd class ECONOMY comment. docs impact: AUTHORING_GUIDE chip section needs no change (behaviour is a safety guard, not a new authored field).
- **verification:** compile-check · **effort:** M
- **risk:** player.gd is a SHARED god-file multiple themes edit — add can_grant_mechanic as a self-contained new method immediately after unlock_mechanic, do NOT touch surrounding functions, and re-read after the Write (open-editor buffer can silently revert). It only READS ABILITY_SCRIPTS + _abilities, no state mutation, so cross-theme risk is low. chip_installer.gd guard must sit before the add_money charge in BOTH transaction paths or the fix is incomplete. New guard changes install_* return semantics only for a previously-broken (unresolvable) id — existing green tests use real ids and stay unaffected.

#### F-T5-3 · [medium] Extend duck-type contract test to all 5 ChessMatch methods + chip install-screen open (C24)
- **files:** tests/test_dialogue_speaker_contracts.gd
- **change:** ChessScreen.open_match calls FIVE methods on the ChessMatch node (ai_search_depth L81, ai_blunder L82, player_is_white L83, wager_amount L84, display_opponent_name L107), but the contract pins only ai_search_depth + display_opponent_name (the two DialogueManager._speaker_chess ALSO scans). Renaming ai_blunder/player_is_white/wager_amount passes the suite and crashes at match start.
1) In COMPONENT_CONTRACTS (L16) extend the chess_match.gd row to all five:
`"res://scripts/components/chess_match.gd": ["ai_search_depth", "display_opponent_name", "ai_blunder", "player_is_white", "wager_amount"],`
2) Add the chip install screen to SCREEN_CONTRACTS (after L24):
`"res://scripts/ui/chip_install_screen.gd": "open_install",`
(ChipInstallScreen already declares `signal closed` L11 and `func open_install` L40, and DialogueManager L428 opens it awaiting closed — so the existing test_screen_open_and_closed_contracts_exist body covers both open method + closed signal with no new code.)
3) Update the module comment (L10-11 / L16 inline comment) to note the two extra chess getters are called by ChessScreen.open_match (not the DM finder), and that the chess row now pins the full open_match surface.
- **test:** This finding IS the test change — no separate test. The two existing loops (test_component_speaker_contracts_exist, test_screen_open_and_closed_contracts_exist) exercise the added entries automatically. Off-tree load().new() with no add_child, per the file's header contract (headless-safe, no _ready). Optionally also add install_carried/install_fee as a ChipInstaller COMPONENT_CONTRACTS row, but that surface is already pinned by test_chip_install.gd::test_installer_exposes_dialogue_duck_type_surface — skip to avoid a duplicate source of truth.
- **docs:** None beyond the in-file comment update. scripts/chess/README.md L41-43 already names this test as the dialogue-seam guard and stays accurate.
- **verification:** gut-editor-closed · **effort:** S
- **risk:** Test-only, no runtime surface. The added chess methods and open_install all exist in code today, so the extended contract is green immediately and simply prevents a future silent rename. No god-file, no money path.

### Batch 13 — T6 cinematic damage immunity: world_frozen() chokepoint + effect freeze
**Fixes:** F-C34, F-C34b  
**Shared god-files:** managers/InputManager.gd, scripts/player/player.gd  
**Why here:** F-C34 adds InputManager.world_frozen() and gates Player.take_damage on it — one guard retires C34 (cutscene never pauses) and C66 (dialogue intro) for hazard/DoT/NPC-fire. F-C34b (depends C34) freezes HazardZone/StatusEffectManager bookkeeping so durations don't burn. Re-touches god-files InputManager.gd (adds method, distinct from T1 rewrite) and player.gd (2-line guard at top of take_damage).  
**Verify:** NOW: compile via -s; off-tree test_world_frozen (has_method, bool, false-at-rest). DEFERRED: the actual damage-immunity + preserved-DoT-duration are playtest-gated (can't flip CutscenePlayer._active off-tree); test_world_frozen.gd sidecar editor-closed.

#### F-C34 · [medium] Cinematic damage immunity: shared world_frozen() predicate + Player.take_damage chokepoint gate (fixes C34 & C66)
- **files:** managers/InputManager.gd, scripts/player/player.gd
- **change:** THE chokepoint fix for both C34 and C66 in one guard. (1) In managers/InputManager.gd add a new INSTANCE method next to gameplay_suppressed() (~after line 101): `func world_frozen() -> bool: return CutscenePlayer.is_active() or DialogueManager.is_engaged()`. Header comment: this is a CINEMATIC control-lock predicate DISTINCT from gameplay_suppressed() (which also counts real-time Pip-Boy/loot/shop overlays that must NOT grant immunity); it is true while a cutscene plays (never pauses the tree) OR any conversation is engaged including the ~0.5s pre-pause intro (dialogue_manager is_engaged() returns _active!=null from intro start) and a suspended sub-menu. Must be an INSTANCE method (autoload), mirroring gameplay_suppressed() (per the autoload-helpers-must-be-instance-methods rule). CutscenePlayer.is_active() is a static call (works); DialogueManager is the autoload global. (2) In scripts/player/player.gd take_damage (line 2033), immediately AFTER the existing `if _dying: return` (line 2034-2035) and BEFORE note_combat() (2036), insert: `if InputManager.world_frozen():` / `	return`. This makes the player ignore ALL incoming damage while control-locked — hazard, DoT, and NPC fire all route through this single override, so one guard covers every vector the cutscene leaks. Player.gd already references InputManager by global name (e.g. line 1751), so the call site is safe. Comment the guard: cutscene deliberately does NOT pause the tree (staged actors keep moving), so immunity rides the predicate, not the pause; a full conversation already pauses the tree, this additionally covers the intro beat (C66).
- **test:** Add tests/test_world_frozen.gd (new): assert the InputManager autoload `has_method(&"world_frozen")`, that `InputManager.world_frozen()` returns a bool, and that it returns `false` at rest (no cutscene/dialogue active in a fresh GUT session). This pins the predicate seam + default. Cannot flip CutscenePlayer._active (private static, no setter) or drive Player.take_damage off-tree (the no-Player-_ready() rule), so the actual damage-immunity BEHAVIOR is playtest-gated (stand in a HazardZone / under fire, trigger a cutscene, confirm HP stops dropping). Use assert_true/assert_eq only (no assert_le/ge). No add_child needed — reference the autoload directly.
- **docs:** docs/CURRENT_ARCHITECTURE.md — add a 'Cinematic damage immunity' line to the pause/cutscene (or combat) contract section: Player.take_damage and the ambient damage sources gate on InputManager.world_frozen() (cutscene active OR a conversation engaged) so a control-locked player takes no hazard/DoT/NPC-fire damage; distinct from gameplay_suppressed(). Optional 1-line comments at cutscene_player.gd:16 and dialogue_manager.gd:137 noting immunity rides world_frozen (kept optional to avoid buffered-editor revert on likely-open authoring files).
- **verification:** compile-check · **effort:** S
- **risk:** player.gd and InputManager.gd are BOTH shared god-files other themes edit — but this fix appends a NEW method (world_frozen) and inserts a 2-line guard at the TOP of take_damage, a region distinct from Theme 2's die()/PickupRay/hotbar/attack edits (C10 is attack.gd; C18 is player.gd:408 die-mid-carry) and Theme 1's _modal_screens/gameplay_suppressed LIST rewrites — low collision, but land it in its own commit touching only these two files (do not sweep the 314 dirty files). Behavior note: this makes the player FULLY invulnerable during any engaged conversation/cutscene; a cutscene that WANTS to script player damage must use a dedicated kill path, not incidental take_damage (acceptable/intended per the findings). Editor-open: only compilation is verifiable now via headless -s; the GUT seam test + playtest are editor-closed follow-ups.

#### F-C34b · [low] Freeze the ambient damage sources (HazardZone, StatusEffectManager) during a cinematic so DoT durations don't burn and burning isn't re-applied
- **files:** scripts/components/hazard_zone.gd, scripts/components/status_effect_manager.gd
- **change:** Completeness pass on the SAME predicate so effect bookkeeping doesn't progress during the freeze (F-C34's take_damage gate nullifies the damage, but without this a poison you walked into silently expires for free during a long cutscene and a fire zone keeps re-applying its on_tick_effect status). (1) scripts/components/hazard_zone.gd _process (line 46): after the existing `if Engine.is_editor_hint() or _bodies.is_empty(): return` (line 47-48) and BEFORE `_tick_t += delta` (line 49), add `if InputManager.world_frozen():` / `	return`. Placing it after the editor-hint guard keeps this @tool node safe in-editor; skipping before `_tick_t += delta` means no catch-up burst when the cinematic ends. Comment: freeze hazard ticking (damage + on_tick_effect status application) for a control-locked body (C34). (2) scripts/components/status_effect_manager.gd _process (line 27-29): change the condition to `if not _active.is_empty() and not InputManager.world_frozen():` before `tick(delta)`. This pauses BOTH DoT damage and duration countdown during a cinematic so effects resume intact afterward. Both edits gate only the per-frame DRIVER (_process), leaving _apply_tick()/tick() pure.
- **test:** none: the existing tests drive the split-out pure methods directly (test_hazard_zone.gd:51/67/84 call h._apply_tick(); test_status_effect.gd:73+ call mgr.tick()), which BYPASS the newly-gated _process — so they stay green with no change. The freeze gate lives in _process and requires a live cutscene/dialogue to exercise, so it is playtest-gated (verify a poison's remaining time is preserved across a cutscene). Confirm the existing two suites are untouched by the diff.
- **docs:** 
- **depends_on:** F-C34
- **verification:** compile-check · **effort:** S
- **risk:** Depends on F-C34 introducing InputManager.world_frozen() first — do not land this before it or the reference won't resolve. StatusEffectManager also runs under NPCs, so this freezes NPC DoT during cinematics too (consistent: during a full dialogue the tree-pause already freezes it for everyone). HazardZone is @tool — the gate is correctly placed AFTER the Engine.is_editor_hint() early-return so nothing runs in-editor. Per-frame InputManager.world_frozen() call is an OR of two cheap state reads — negligible even across many hosts; not a byte-order-critical path. Touch only these two component files.

### Batch 14 — T7 NPC AI: retarget throttle, give-up settle, target-less flee, corpse/reload gates
**Fixes:** F-C8, F-C36, F-C6, F-C7, F-C5, F-C35  
**Shared god-files:** scripts/npc/npc.gd  
**Why here:** npc.gd cluster (adjacent to Batch 15 to minimise re-read churn). Ordered per theme note: F-C8 first (throttle fix + NpcTargeting._should_immediately_retarget), then F-C36 (give-up latch settle, depends C8; edits the same 1789-1859 dispatch region without line overlap). F-C6 (target-less flee), F-C7 (UNAWARE-only corpse discovery), F-C5 (weapon_stance reload supply), F-C35 (GoapProfile.validate) are independent single-region edits.  
**Verify:** NOW: compile via -s; off-tree GUT (targetless retarget false, flee uses last_known_position, settle clears latches, GoapProfile hp/temperament validation). DEFERRED: in-tree corpse-discovery/flee/reload/give-up behaviors playtest; new test files' sidecars editor-closed.

#### F-C8 · [high] Retarget throttle no longer runs an every-frame O(n) scan for target-less NPCs
- **files:** scripts/npc/npc.gd, scripts/npc/npc_targeting.gd
- **change:** ROOT CAUSE: at npc.gd:1789-1792 the throttle is `if _retarget_timer <= 0.0 or _target_invalid():`. `_target_invalid()` (npc_targeting.gd:37) returns true whenever `_is_live(host._target)` is false — which includes `_target == null`. So every idle/target-less NPC (most of the cast) fails the O(1) pre-check every physics frame and pays the full `_acquire_target()` group scan (two get_nodes_in_group loops + distance tests) each frame. FIX (treat no-target as valid-idle): (1) Add to npc_targeting.gd a new method `func _should_immediately_retarget() -> bool:\n\treturn is_instance_valid(host._target) and _target_invalid()` with a docstring: only pay for an IMMEDIATE re-acquire when we currently HOLD a target instance (valid, or a downed-in-tree character like the revive-in-place player) that just went invalid; a target-LESS NPC returns false and re-scans only on the throttle timer, so the idle cast stays O(1)/frame (a new foe is still picked up within retarget_interval, default 0.5s). is_instance_valid short-circuits so the transform reads inside _target_invalid never run for a null/freed target. (2) Add a host facade in npc.gd next to _target_invalid (after line 2298): `func _should_immediately_retarget() -> bool:\n\treturn _targeting._should_immediately_retarget() if _targeting != null else false` (off-tree default false: no _targeting child -> no scan). (3) Change npc.gd:1790 from `if _retarget_timer <= 0.0 or _target_invalid():` to `if _retarget_timer <= 0.0 or _should_immediately_retarget():`. Update the adjacent 1786-1788 comment to say the immediate branch fires only for a HELD-but-invalid target; a target-less NPC re-scans on the timer.
- **test:** Add to tests/test_target_liveness.gd an inner `class _HostStub extends Node:` with `var _target = null`, plus `func test_targetless_npc_does_not_force_immediate_retarget() -> void:` — `var tg := NpcTargeting.new(); var stub := _HostStub.new(); tg.host = stub; assert_false(tg._should_immediately_retarget(), "no target -> valid-idle, no every-frame O(n) scan (C8)"); tg.free(); stub.free()`. This exercises the null short-circuit WITHOUT any global_position read (GUT 9.6-safe, per the aim_distance precedent). The held-but-invalid immediate path is in-tree (needs transform reads) -> playtest.
- **docs:** scripts/npc/README.md — add `_should_immediately_retarget` to the NPC host-facade contract (the retarget-throttle seam). Update the npc.gd:1786-1792 comment.
- **verification:** gut-editor-closed · **effort:** M
- **risk:** SHARED GOD-FILE npc.gd (multiple themes edit it) — this touches the physics_process retarget region (1786-1792) that F-C36 also edits (branch at 1798-1841); apply both in one coordinated pass, they do not overlap line-for-line. Behavior change: a target that FREES mid-combat (an NPC ally dying) now re-acquires within retarget_interval (0.5s) instead of same-frame. The player is unaffected (revive-in-place stays in-tree -> is_instance_valid true -> immediate). Acceptable per the review's 'valid-idle' guidance.

#### F-C36 · [medium] Give-up bark latches reset when the target dies/frees (no cross-engagement leak)
- **files:** scripts/npc/npc.gd
- **change:** ROOT CAUSE: the give-up latches _saw_combat/_was_aware (npc.gd:337-338) are only cleared by the has-target elif at 1834-1841 (perception drops to UNAWARE with a still-valid target). When the target DIES/FREES mid-engagement, the next frame takes the no-target branch (1798) and returns at 1811 — the reset never runs, so _saw_combat/_was_aware stay latched. At the START of the next engagement, the first frame (proximity target acquired but perception still UNAWARE) hits `elif _was_aware:` and fires _try_combat_end_bark() spuriously (invisible today only because bark pools ship empty). FIX (centralize the settle): (1) add `func _settle_engagement_barks() -> void:` — `if not _was_aware: return` then `if _saw_combat: _try_combat_end_bark() else: _try_lost_interest_bark()` then clear `_saw_combat = false; _was_aware = false; _alerted_allies = false`. Docstring: fires the give-up bark at most once per engagement and prevents latch leak, called from BOTH the perception-drop (has-target) and target-lost (no-target) paths (C36). (2) Replace the has-target `elif _was_aware:` block (1834-1841) with `else:\n\t_settle_engagement_barks()` (the helper's internal `_was_aware` guard preserves the original behavior). (3) In the no-target branch, add `_settle_engagement_barks()` as the FIRST statement (before `_react_unaware(delta)` at ~1805). The bark helpers null-guard on _voice (1544-1552) so this is side-effect-free off-tree and when un-voiced.
- **test:** Add tests/test_npc_give_up_barks.gd: `func test_settle_clears_engagement_latches() -> void:` — build NPC off-tree (`load("res://scripts/npc/npc.gd").new()`, no _ready, _voice stays null so barks no-op), set `e._was_aware = true; e._saw_combat = true; e._alerted_allies = true`, call `e._settle_engagement_barks()`, assert all three are false. Second case: with `_was_aware = false` set a sentinel-friendly check that calling it is a no-op (flags stay false). Off-tree, GUT 9.6-safe (no transforms touched).
- **docs:** Comment near npc.gd:1821-1841 (the give-up bark bookkeeping) documenting the two call sites. scripts/npc/README.md if it enumerates engagement latches (it does not currently).
- **depends_on:** F-C8
- **verification:** gut-editor-closed · **effort:** M
- **risk:** SHARED GOD-FILE npc.gd — co-edits the physics_process branch region (1798-1841) that F-C8 touches at 1789-1792. Apply F-C8 first, then this, in one pass; lines do not overlap. This depends on the target being nulled promptly on death: F-C8's throttle (or the pre-existing _target_invalid path) re-acquires -> _set_target(null) -> the no-target branch runs the settle. Double-clearing _alerted_allies (also cleared in _react_unaware:1870) is harmless.

#### F-C6 · [high] Target-less FLEE no longer null-derefs _aim_point()
- **files:** scripts/npc/npc_locomotion.gd, scripts/npc/npc.gd
- **change:** ROOT CAUSE: npc_locomotion.gd:121 `_act_flee` does `host.global_position - host._aim_point()`. `_aim_point()` (npc.gd:2326-2328) is `node.global_position + ...` where `node = _target_body if valid else _target` — a hard null deref when BOTH are null. A scripted `investigate()` (npc.gd:2256, e.g. squad sweep / alarm) sets perception INVESTIGATING with NO _target; GoapActionFlee.is_runtime_valid passes (is_fleeing + state != UNAWARE), so the executor runs Flee.act -> _act_flee -> _aim_point() crash + frozen NPC every frame. FIX (flee from last_known_position): (1) Add to npc.gd, immediately before _aim_point (line 2326), `func _flee_threat_point() -> Vector3:` returning `var node: Node3D = _target_body if is_instance_valid(_target_body) else _target` then `if is_instance_valid(node): return node.global_position + Vector3.UP * target_height`, then `if _perception != null: return _perception.last_known_position` (perception is non-UNAWARE on any flee reachable here, so investigate_point/alert_to has seeded last_known_position — perception.gd:220), else `return global_position` (the _act_flee 'standing on the threat' branch then bolts spawn-ward). Docstring: explains this is the flee-only threat accessor that survives a target-less flee, distinct from the combat-hot _aim_point. (2) In npc_locomotion.gd:121 change `host._aim_point()` to `host._flee_threat_point()`. Do NOT change _aim_point itself (it is on hot combat paths at npc.gd:990/1829/2636/2706 that always have a target) and do NOT gate GoapActionFlee out — a fleer investigating a noise should still run.
- **test:** Add tests/test_npc_flee_targetless.gd: `const NPC_PATH := "res://scripts/npc/npc.gd"`, `const PERC_PATH := "res://scenes/enemies/perception.gd"`. `func test_flee_threat_point_uses_last_known_when_targetless() -> void:` — `var e = load(NPC_PATH).new(); var perc = load(PERC_PATH).new(); perc.last_known_position = Vector3(5,0,-3); e._perception = perc; assert_eq(e._flee_threat_point(), Vector3(5,0,-3), "target-less flee runs from last_known_position, not a crash (C6)"); perc.free(); e.free()`. Off-tree via load().new() (no _ready); the last-known path reads NO transform, so it is GUT 9.6-safe. The global_position fallback (no perception) and the target-present path both read transforms -> not off-tree-testable; verify by inspection + playtest.
- **docs:** scripts/npc/README.md — add `_flee_threat_point` to the NpcLocomotion facade row (line 66, alongside `_aim_point`). Comment in npc_locomotion.gd:118-121 noting the target-less-flee contract.
- **verification:** gut-editor-closed · **effort:** M
- **risk:** SHARED GOD-FILE npc.gd (new method inserted near 2326 — a low-traffic region, no overlap with F-C7/C8/C36). last_known_position defaults to Vector3.ZERO but is always seeded before a non-UNAWARE flee, so no false (0,0,0) flee target in practice.

#### F-C7 · [medium] Stale-DETECTING NPC no longer permanently consumes a corpse without investigating
- **files:** scripts/npc/npc.gd
- **change:** ROOT CAUSE: npc.gd:1906 `if _perception.state != Perception.State.INVESTIGATING and corpse_on:` then _discover_corpse (1981) which does `c.discovered = true` + `GameState.mark_corpse_discovered(...)` (PERSISTED). The `!= INVESTIGATING` guard still lets a stale DETECTING/ALERTED state through (a just-lost target leaves perception non-UNAWARE for a beat), so a body is marked permanently discovered with zero real investigation. FIX (restrict to UNAWARE, per review): change npc.gd:1906 to `if _perception.state == Perception.State.UNAWARE and corpse_on:`. In the no-target branch _perception.sense(delta) (called at 1892 with is_hostile=false) decays a stale DETECTING toward UNAWARE, so discovery is deferred (not lost) until the NPC is genuinely idle. Update the nearby comment to explain the UNAWARE restriction and why (persisted side-effect must not fire off a stale combat state).
- **test:** none: in-tree perception + NpcSenses group-scan/LOS + GameState + a Corpse — the existing body-discovery testing note (npc.gd:1868) marks this path playtest-verified. A pure off-tree test can't reach _nearest_visible_corpse. Verify the state gate by inspection + playtest (lose a target near a fresh corpse; body is NOT auto-marked discovered until the NPC fully stands down).
- **docs:** Comment at npc.gd:1906. The CLAUDE.md corpse-discovery persistence note already documents GameState.discovered_corpses; no doc contradiction to fix.
- **verification:** playtest-gated · **effort:** S
- **risk:** SHARED GOD-FILE npc.gd (isolated one-line change at 1906, distinct region from C8/C36). Behavior: body discovery is now slightly deferred (waits for UNAWARE) — intended.

#### F-C5 · [medium] Out-of-combat reload no longer dry-clicks every frame / never holsters on empty reserve
- **files:** scripts/npc/weapon_stance.gd
- **change:** ROOT CAUSE: weapon_stance.gd:91 `if _has_engaged and host._weapon.current_ammo < max_ammo and not host._weapon.is_busy():` draws + calls reload() every frame. With a PARTIAL clip (current_ammo>0 so _can_fight_with_gun stays true and the holster-early-return at 74 doesn't fire) but EMPTY reserve, reload() is a no-op dry click (ammo.gd:100-101 keeps the partial clip; comment at ammo.gd:83 confirms the dry click), so the branch re-fires every frame — SFX spam — and the holster branch (98) is never reached. FIX (gate on reload supply): add helper `func _has_reload_supply() -> bool:\n\treturn host._weapon != null and host._weapon.ammo != null and host._weapon.ammo.has_reload_supply()` (has_reload_supply already exists, ammo.gd:84). Change line 91 to `if _has_engaged and host._weapon.current_ammo < max_ammo and _has_reload_supply() and not host._weapon.is_busy():`. When there's no supply the branch is skipped; a predisposed-hostile NPC keeps its gun out via the `always_out` elif (95), everyone else stands down and holsters via the elif at 98. The NPC keeps fighting its partial clip (still has rounds); once dry with no reserve _can_fight_with_gun() flips false and holster+fists takes over as before.
- **test:** Optional source-string contract in tests/test_npc_facade_contract.gd (or a new tiny test): assert the weapon_stance.gd source contains `_has_reload_supply()` inside the reload branch, mirroring the existing `contains("host.set_last_attacker(")` idiom — cheap guard against the gate being dropped. has_reload_supply's own truth table is already covered by tests/test_ammo_reserve.gd. The reconcile() draw/holster loop is in-tree -> playtest (partial-clip enemy you've stripped of spare ammo stops clicking and stows its gun).
- **docs:** Comment at weapon_stance.gd:90-94 explaining the empty-reserve dry-click guard (C5).
- **verification:** playtest-gated · **effort:** S
- **risk:** Isolated to weapon_stance.gd (not a shared god-file). host._weapon.ammo is null-guarded so off-tree/partly-built stays safe.

#### F-C35 · [medium] GoapProfile.validate() also checks hp_scales / temperament_scales rows
- **files:** scripts/npc/goap/goap_profile.gd
- **change:** ROOT CAUSE: validate() (goap_profile.gd:88-103) loops goal_priorities, action_cost_overrides, and goals — but NOT hp_scales (line 31) or temperament_scales (line 35), both `Array[GoapGoalPriority]` with the same `row.goal` shape consumed by hp_scale_for/temperament_scale_for. A typo'd goal name in either array silently no-ops (the lookup never matches -> falls to fallback 0.0 -> no scaling), the exact silent-failure validate exists to catch. FIX: after the action_cost_overrides loop (before the `goals` loop at line 98), add two loops mirroring the goal_priorities loop: `for row in hp_scales:` / `if row != null and not known_goals.has(row.goal): push_warning("GoapProfile: hp_scales row '%s' matches no known goal — hp scaling ignored." % row.goal); ok = false`, and the same for `temperament_scales` with a temperament-worded message. Update the class docstring (lines 12-13) so 'any row' explicitly includes hp/temperament scales.
- **test:** Add a test to tests/test_npc_goap_library.gd modeled on test_goap_profile_validate_against_the_real_library (line 108): build goal/action name arrays from npc._build_goap_goals()/_build_goap_actions(), then `prof.hp_scales.assign([_gpri("Survive", 2.0)]); assert_true(prof.validate(goal_names, action_names), ...)`; `prof.hp_scales.assign([_gpri("Suvrive", 2.0)]); assert_false(prof.validate(...), "a typo'd hp_scales goal fails validation (C35)")`; repeat for temperament_scales. Uses the existing _gpri helper (line 59). Off-tree, no _ready.
- **docs:** goap_profile.gd docstring (12-13). If docs/AUTHORING_GUIDE.md documents GoapProfile validation coverage, note hp/temperament rows are now validated.
- **verification:** gut-editor-closed · **effort:** S
- **risk:** @tool Resource script — an editor-open buffered edit can silently revert (re-read after saving). No runtime hot path. Isolated file.

### Batch 15 — T8 faction/rep: exact provoke-reversal, rep-neutral auto-aggro, content guards
**Fixes:** F-C32, F-C33, F-C64, F-C65  
**Shared god-files:** scripts/npc/npc.gd, managers/Reputation.gd  
**Why here:** Kept adjacent to Batch 14 as npc.gd's final cluster. F-C32 (Reputation._apply_delta extraction + adjust_unscaled; npc.gd captures/reverses the exact provoke delta on forgive) first — it defines the seam F-C33's docs reference. F-C33 (EncounterSpawner auto-aggro with apply_rep=false) is independent. F-C64 (drop malformed neutral_wildlife.tres uid) and F-C65 (shipped-raiders-hostile content test) are leaf fixes.  
**Verify:** NOW: compile via -s; off-tree tests (holster restores exact rep + stands down, adjust_unscaled round-trip, _aggro_spawn passes apply_rep=false, shipped raiders HOSTILE at rep 0). DEFERRED: neutral_wildlife.tres uid REGENERATION needs an editor reimport (editor-closed); GUT run editor-closed.

#### F-C32 · [high] forgive_provoke restores the exact faction rep the provoke dropped (holster truly de-escalates a factioned NPC)
- **files:** managers/Reputation.gd, scripts/npc/npc.gd, docs/AUTHORING_GUIDE.md, tests/test_hostility.gd, tests/test_faction.gd
- **change:** Root cause: for a factioned NPC, resolved_kind priority is provoked>faction-rep; provoke() drops faction rep by provoke_penalty (30, streetwise-scaled) and with hostile_threshold=-25 the FIRST provoke crosses into HOSTILE. forgive_provoke() clears _provoked but leaves rep at -30, so disposition_for() still returns HOSTILE — holstering can never stand the faction down. A pure tuning change can't fix it robustly because a negative-streetwise player's rep_loss_mult>1.0 amplifies the drop past any provoke_penalty<|hostile_threshold| margin. Fix = make forgive exactly reverse the rep the provoke applied. (1) managers/Reputation.gd: extract the clamp+store+signal core of add_reputation() into a private `func _apply_delta(faction: Faction, delta: float) -> float` (the body currently at lines 42-55: read before_kind, before, clamp before+delta to [rep_min,rep_max], store, compute actual_delta, emit reputation_changed + alignment_changed on cross, return total). add_reputation() keeps its streetwise scaling (lines 38-41) then `return _apply_delta(faction, delta)`. Add a new public `func adjust_unscaled(faction: Faction, delta: float) -> float:` that null-guards then `return _apply_delta(faction, delta)` — an UNSCALED, clamp/signal-correct delta used to reverse a provoke exactly (comment: not streetwise-scaled, since it undoes an already-scaled hit). (2) scripts/npc/npc.gd: add member `var _provoke_rep_delta: float = 0.0` right after `var _provoked` (line 112) with a comment: the exact (streetwise-scaled, clamp-aware) rep delta this NPC's provoke applied, so forgive can reverse it; runtime-only. In provoke() inside the `if apply_rep and faction != null:` block (lines 926-928), capture the actual applied delta: `var before := Reputation.get_reputation(faction)` then `_provoke_rep_delta = Reputation.add_reputation(faction, -provoke_penalty) - before` (add_reputation returns the new total). In forgive_provoke() (after clearing _provoked at line 938, before/after _apply_outline): `if faction != null and _provoke_rep_delta != 0.0: Reputation.adjust_unscaled(faction, -_provoke_rep_delta)` then `_provoke_rep_delta = 0.0`. This works faction-wide (the shared pool restores, so every same-faction member re-reads above threshold) and across multiple provoked members (each stored its own clamp-aware delta; the sum round-trips). Update the forgive_provoke docstring (lines 932-934) to state it also restores the faction rep the provoke dropped; note KILL penalties are NOT reversed (holster only forgives a provoke). Update npc.gd provoke() comment to mention the delta is captured for reversal.
- **test:** tests/test_hostility.gd: add `test_holstering_forgives_the_provoke_rep_hit()` — off-tree NPC (load(ENEMY_PATH).new()), faction townsfolk NEUTRAL, `var before := Reputation.get_reputation(f)` (before_each already Reputation.reset()), e.provoke(); assert_true(e.is_hostile()); e.forgive_provoke(); assert_false(e._provoked); assert_eq(Reputation.get_reputation(f), before, 'forgive restores the exact rep the provoke dropped'); assert_false(e.is_hostile(), 'flag cleared + rep restored -> stands down'); e.free(). (No live player in the test -> add_reputation is unscaled, so it round-trips to 0.) tests/test_faction.gd: add `test_adjust_unscaled_reverses_a_delta_exactly()` on a fresh Reputation (_fresh_rep()): add_reputation(f,-30.0); adjust_unscaled(f,30.0); assert_eq(get_reputation(f),0.0); rep.free(). Existing test_provoke_drops_faction_reputation / test_already_hostile_does_not_double_drop_rep stay green (provoke's drop is unchanged; capture only happens inside the existing `if not _provoked`+`if apply_rep` guards).
- **docs:** docs/AUTHORING_GUIDE.md line 957: rewrite the trailing 'provoke drops the whole faction's player-rep by provoke_penalty (FNV-style), so the group sours together' clause to add that HOLSTERING (weapon down) forgives a provoke — it clears the transient flag AND restores the exact rep the provoke removed, so a whole faction stands back down; a genuinely-hostile-by-rep faction (soured by KILLS, not just a provoke) stays hostile because kill_penalty is not reversed. Also update the npc.gd forgive_provoke + provoke code comments in the same diff.
- **verification:** compile-check · **effort:** M
- **risk:** npc.gd is a shared god-file multiple themes edit — keep changes to the one new member + provoke()/forgive_provoke() bodies only; do not reflow surrounding code. Reputation.gd refactor MUST preserve add_reputation's exact public behavior (streetwise scaling THEN clamp/signal, same emit order) — _apply_delta is a pure extraction; verify the streetwise path (lines 38-41) still runs before _apply_delta. No byte-order/serialization hot path. Editor-open: npc.gd/Reputation.gd are .gd so headless -s proves compile; a buffered editor copy could silently revert — re-grep after writing.

#### F-C33 · [high] EncounterSpawner auto_aggro provokes with apply_rep=false (no per-member squad-size rep multiplication)
- **files:** scripts/components/encounter_spawner.gd, docs/AUTHORING_GUIDE.md, tests/test_encounter_spawner.gd
- **change:** encounter_spawner.gd:80-81 calls `npc.provoke(_player())` per spawned member with the default apply_rep=true, so an N-member auto_aggro wave drops faction rep by N x provoke_penalty in one trigger — the exact GA-3 multiplication provoke()'s own docstring (npc.gd:918-922) and alarm_panel guard against. An authored ambush shouldn't tank the player's faction rep at all (the player hasn't attacked anyone; kills still apply kill_penalty later via the death path). Fix: spawn hostile but with NO rep drop. Extract the aggro call into a static, duck-typed, injectable helper mirroring AlarmPanel.aggro_faction_members: `static func _aggro_spawn(npc: Node, player: Node) -> void:` body `if npc != null and npc.has_method(&"provoke"): npc.provoke(player, false)` with a comment explaining apply_rep=false avoids the squad-size multiplication and that an authored ambush costs no rep (the tripping event — e.g. an AlarmPanel — owns any one-time rep hit). Replace lines 80-81 with `if def.auto_aggro: _aggro_spawn(npc, _player())`. Update the _spawn_one docstring (line 61-62) which says 'aggro it onto the player when asked' to note it's rep-neutral.
- **test:** tests/test_encounter_spawner.gd: add an inner recorder stub `class _ProvokeRec extends Node: var provoke_count:=0; var last_apply_rep:=true; func provoke(_a=null, apply_rep:=true)->void: provoke_count+=1; last_apply_rep=apply_rep` and `test_auto_aggro_provokes_without_dropping_rep()`: `var rec := _ProvokeRec.new(); EncounterSpawner._aggro_spawn(rec, null); assert_eq(rec.provoke_count, 1, 'auto_aggro provokes the spawn'); assert_false(rec.last_apply_rep, 'must provoke with apply_rep=false so an N-member wave doesnt multiply the faction rep hit'); rec.free()`. Mirrors test_alarm_panel's stub pattern; no real NPC _ready. (The full _spawn_one path still instantiates NPC scenes -> stays playtest-verified per the file's header note.)
- **docs:** docs/AUTHORING_GUIDE.md line 539: extend the auto_aggro bullet — 'make each spawn immediately hostile ... on arrival' — to add that spawning does NOT cost the player faction reputation (rep-neutral; wire an AlarmPanel if you want a one-time faction rep hit, which applies it exactly once).
- **verification:** compile-check · **effort:** S
- **risk:** Isolated component file — does NOT touch npc.gd/Reputation.gd, so no collision with F-C32. provoke(.., false) is an existing, alarm_panel-exercised signature. Low risk. Editor-open: .gd compiles headless; re-grep after write for a buffered revert.

#### F-C64 · [low] neutral_wildlife.tres: drop the hand-written malformed UID and let the editor regenerate it
- **files:** resources/factions/neutral_wildlife.tres
- **change:** Line 1 hand-writes `uid="uid://faction_wild000"` — an invalid, non-editor-generated UID (per project rule: never hand-write uid:// strings; a uid-less .tres header is fine, Godot fills it on next import). Change line 1 from `[gd_resource type="Resource" script_class="Faction" format=3 uid="uid://faction_wild000"]` to `[gd_resource type="Resource" script_class="Faction" format=3]` (drop only the uid= token; leave type/script_class/format untouched). Verified nothing references this uid: grep for `faction_wild000` finds only this file + the review doc — factions resolve by PATH (Factions.by_id path-load) and NPCs reference by faction_id string, never by uid, so removing it breaks no reference.
- **test:** none: content .tres UID header, no runtime surface. Existing tests/test_faction.gd::test_factions_ids_scans_disk_sorted already asserts 'neutral_wildlife' is present via filename scan (uid-independent), so the regen is implicitly covered.
- **docs:** none (data-only header fix).
- **verification:** gut-editor-closed · **effort:** S
- **risk:** Editor is OPEN = import lock: the editor will regenerate the UID (and write a .tres.uid / rewrite the header) only on the NEXT reimport, which can't happen this session; a buffered editor copy could also silently revert the edit. Safe to write now, but the real UID regeneration is editor-closed work — verify by reopening the editor and confirming the header gained a fresh uid://. No other resource references the old uid, so there is no dangling-reference window.

#### F-C65 · [medium] Content test: shipped raiders.tres is HOSTILE-on-sight at rep 0 (guards against a silent NEUTRAL flip)
- **files:** tests/test_faction.gd
- **change:** The existing hostility tests build SYNTHETIC raiders via _faction(&"raiders", HOSTILE) (test_npc_vs_npc.gd) or hand-set default_disposition (test_faction.gd::test_hostile_default_faction_reads_hostile_at_zero_rep), so none pins the SHIPPED resources/factions/raiders.tres — a designer flipping it to NEUTRAL (default_disposition 0->1) would pass every test while breaking on-sight raider combat. Add a content test in test_faction.gd's 'Faction content guards' section that reads the shipped file via Factions.by_id: `func test_shipped_raiders_are_hostile_on_sight_at_zero_rep() -> void:` — `var raiders := Factions.by_id("raiders"); assert_not_null(raiders, "raiders.tres must exist"); assert_eq(raiders.default_disposition, Disposition.Kind.HOSTILE, "shipped raiders.tres must be HOSTILE-by-default so raiders fight on sight at rep 0"); var rep := load(REPUTATION_PATH).new(); assert_eq(rep.disposition_for(raiders), Disposition.Kind.HOSTILE, "at rep 0 (neutral band) the shipped raiders faction must resolve HOSTILE"); rep.free()`. Uses the existing REPUTATION_PATH const + Factions preload already in the file.
- **test:** This fix IS the test (above). Off-tree: Factions.by_id is a path-load, disposition_for runs on a fresh Reputation instance reading the GameSettings.reputation autoload thresholds (available headless). No _ready, no add_child. Uses assert_eq (not banned assert_le/ge).
- **docs:** none (test-only; AUTHORING_GUIDE line 953-955 already documents the rep->disposition mapping the test pins).
- **verification:** gut-editor-closed · **effort:** S
- **risk:** Pure additive test, no production code. Independent of F-C64 (by_id is uid-independent). Editor-open: cannot run GUT this session — compiles under headless -s; actual pass is editor-closed. Only fires red if a designer genuinely flips raiders.tres, which is the intended guard.

### Batch 16 — T9 designer-first de-duplication: restamp chokepoint, STAT_NAMES master, audio/explosion/probe
**Fixes:** F-C61, F-CP1, F-C63, F-C31, F-C62  
**Shared god-files:** scripts/player/character_stats.gd, scripts/components/level_up.gd, managers/GameState.gd  
**Why here:** F-C61 (CharacterStats.restamp_derived single chokepoint) MUST precede F-CP1 (STAT_NAMES master const) — both edit character_stats.gd AND level_up.gd, so serialize to one pass (F-CP1 also re-touches god-file GameState.gd with a one-line const swap). F-C63 (NPC impact dB → GameSettings.audio), F-C31 (probe to slider max), F-C62 (Explosion.instantiate_recovering) are independent, disjoint files.  
**Verify:** NOW: compile via -s (confirm the cross-class const fold resolves); off-tree tests (restamp floors/telescopes + returns applied delta, all STAT_NAMES mirrors ==, Explosion.instantiate_recovering not-null). Existing level_up/perk/passive tests must stay byte-identical. DEFERRED: Pet/Claim probe range + FX are playtest; GUT run editor-closed.

#### F-C61 · [high] Extract the strength->max_hp/carry re-stamp into one CharacterStats.restamp_derived chokepoint
- **files:** scripts/player/character_stats.gd, scripts/components/level_up.gd, scripts/components/perk_manager.gd, scripts/components/passive_item_buffs.gd, tests/test_player_stats.gd
- **change:** The strength-derived HP/carry re-stamp is hand-rolled in 3 places and has drifted (LevelUp: raw hp move, no clamp/floor/signal; PerkManager: clamps max_hp>=1 + hp, no carry floor, no signal; PassiveItemBuffs: clamps max_hp>=1, floors carry>=0, moves hp by real max change, emits `damaged`, tracks post-floor delta). Add ONE static to character_stats.gd (it already owns HP_PER_STRENGTH/CARRY_PER_STRENGTH + max_hp_bonus()/carry_bonus()):

`static func restamp_derived(host: Object, hp_delta: float, carry_delta: float, old_stamina_max: float = -1.0) -> Vector2:`
  - if host == null: return Vector2.ZERO
  - if hp_delta != 0.0 and host.get(&"max_hp") != null: old_max=float(host.get(&"max_hp")); new_max=maxf(1.0, old_max+hp_delta); host.set(&"max_hp",new_max); host.set(&"hp", clampf(float(host.get(&"hp"))+(new_max-old_max), 1.0, new_max)); applied.x=new_max-old_max; if host.has_signal(&"damaged"): host.emit_signal(&"damaged", float(host.get(&"hp")), new_max)
  - if carry_delta != 0.0 and host.get(&"carry_capacity") != null: old_carry=float(host.get(&"carry_capacity")); new_carry=maxf(0.0, old_carry+carry_delta); host.set(&"carry_capacity",new_carry); applied.y=new_carry-old_carry
  - if old_stamina_max >= 0.0 and host.has_method(&"apply_stamina_max_delta"): host.call(&"apply_stamina_max_delta", old_stamina_max)
  - return applied  (the ACTUALLY-applied post-floor deltas)

Then rewire the 3 call sites, each keeping its own private-sheet ownership + before/after bonus-diff (capture old_stamina_max BEFORE mutating the sheet — stamina reads endurance):
- level_up.gd level_up_stat (lines 89-97): after `stats.set(stat, ...)`, replace the 4 manual lines (max_hp+=, hp+=, carry_capacity+=, apply_stamina_max_delta) with a single `CharacterStats.restamp_derived(player, stats.max_hp_bonus()-old_hp_bonus, stats.carry_bonus()-old_carry_bonus, old_stamina_max)`.
- perk_manager.gd _apply_stat_bonuses (lines 128-137) AND _reverse_stat_bonuses (lines 179-186): replace each manual max_hp/hp/carry/stamina block with `CharacterStats.restamp_derived(host, stats.max_hp_bonus()-old_hp, stats.carry_bonus()-old_carry, old_stamina_max)`.
- passive_item_buffs.gd: change `_restamp(str_points)` signature to `_restamp(str_points: float, old_stamina_max: float)`; inside, replace the whole hp/carry body (lines 133-149) with `var applied := CharacterStats.restamp_derived(_host, target_hp-_applied_hp, target_carry-_applied_carry, old_stamina_max); _applied_hp += applied.x; _applied_carry += applied.y`. In _recompute, pass old_stamina_max into the call and DELETE the now-redundant separate apply_stamina_max_delta block (lines 118-119).
This fixes the LevelUp drift (it now clamps/floors/signals like the others) with zero number change for normal positive builds.
- **test:** tests/test_player_stats.gd: add test_restamp_derived_floors_heals_and_returns_applied_delta using a bare Node stub (fields max_hp/hp/carry_capacity, NO damaged signal, NO apply_stamina_max_delta — mirror test_passive_item_buffs._StubHost). Assert: positive delta raises max+heals hp and returns the delta; a negative delta into the max_hp<1 / carry<0 floor returns the REAL post-floor applied delta (not the ideal) so it telescopes. Existing test_level_up.gd (101.5/12.0/heal), test_passive_item_buffs.gd (13.0 max, wounded-clamp-to-2, negative-strength-floor-telescope), and test_perks.gd must still pass byte-for-byte (verify off-tree via load(PLAYER_PATH).new() — Player HAS the damaged signal + apply_stamina_max_delta, so emitting off-tree is harmless).
- **docs:** Update the class comment on character_stats.gd (lines ~29-37, the strength-restamp exception paragraph) to name restamp_derived as the single chokepoint; trim the now-duplicated 'mirrors LevelUp/PerkManager' prose in passive_item_buffs.gd (lines 121-124) and perk_manager.gd (lines 107-109) to point at restamp_derived instead. docs/CURRENT_ARCHITECTURE.md if it documents the level-up/perk/held-buff stat pipeline.
- **verification:** gut-editor-closed · **effort:** M
- **risk:** Touches scripts/player/character_stats.gd + scripts/components/level_up.gd — the SAME two files as F-CP1 (serialize). Behavior change: LevelUp and PerkManager now emit `damaged` on a stat raise/perk (they previously did not) — this is a HUD refresh on heal, matching Character.heal semantics; confirm no `damaged` listener treats it as pure damage. apply/reverse-inverse property in PerkManager is preserved because restamp uses identical maxf/clampf floors. Duck-typed host: NPC hosts lack stamina_max/apply_stamina_max_delta, so the has_method guard is load-bearing.

#### F-CP1 · [high] Make GameState / LevelUp / character_creation stat-name lists derive from one CharacterStats.STAT_NAMES master const
- **files:** scripts/player/character_stats.gd, managers/GameState.gd, scripts/components/level_up.gd, scripts/ui/character_creation.gd, tests/test_player_stats.gd
- **change:** The stat-name list is hand-mirrored in 4 places; a name missing from GameState.STAT_NAMES silently drops that stat from every save. Make CharacterStats the true single source:
1. character_stats.gd: add `const STAT_NAMES: Array[StringName] = [&"strength", &"endurance", &"gunplay", &"agility", &"streetwise", &"stealth", &"pickpocket"]` and rewrite `stat_names()` to derive: `return PackedStringArray(STAT_NAMES)` (keeps its PackedStringArray return + stat_names_csv() callers working).
2. GameState.gd:36: replace the literal with `const STAT_NAMES: Array[StringName] = CharacterStats.STAT_NAMES` (the 4 `for n in STAT_NAMES` loops at lines 211/302/365/558 keep working — same typed array).
3. level_up.gd:19: replace with `const STAT_NAMES: Array[StringName] = CharacterStats.STAT_NAMES` (total_level + the `stat in STAT_NAMES` guard keep working).
4. character_creation.gd:34: replace with `const STATS: Array[StringName] = CharacterStats.STAT_NAMES`.
Referencing another class_name's const in a const initializer is a compile-time constant fold — no cycle (CharacterStats is a pure Resource that references none of these three).
- **test:** tests/test_player_stats.gd: add test_stat_name_mirrors_share_one_source asserting `GameState.STAT_NAMES == CharacterStats.STAT_NAMES`, `LevelUp.STAT_NAMES == CharacterStats.STAT_NAMES`, and `CharacterCreation.STATS == CharacterStats.STAT_NAMES` (load the character_creation script via load('res://scripts/ui/character_creation.gd') to read its STATS const off the GDScript). Keep the existing test_stat_names_round_trip_through_get_stat (size==7 + get_stat round-trip) as the add-a-name-without-get_stat guard.
- **docs:** Fix the now-stale 'mirrors' comments that claim hand-mirroring: GameState.gd:34-35, level_up.gd (the STAT_NAMES line), character_creation.gd:32-33 — reword each to 'derived from CharacterStats.STAT_NAMES (the single source; cannot drift)'. Update the stat_names() docstring on character_stats.gd:92-94 to say STAT_NAMES is the master and stat_names() is the PackedStringArray view.
- **depends_on:** F-C61
- **verification:** gut-editor-closed · **effort:** S
- **risk:** GameState.gd is a shared god-file multiple themes touch and an AUTOLOAD — but this is a one-line const swap + comment, no logic. Shares character_stats.gd AND level_up.gd with F-C61: land F-C61 first, apply this in the same pass to avoid a same-file revert (editor open can silently drop a buffered edit). Compile-check must confirm the cross-class const fold resolves (CharacterStats is already in the global class cache — no NEW class_name added).

#### F-C63 · [medium] Move the duplicated NPC_IMPACT_VOLUME_DB const into GameSettings.audio (designer-tunable)
- **files:** resources/tuning/AudioSettings.gd, scripts/combat/weapon_audio.gd, scripts/projectiles/projectile.gd
- **change:** NPC_IMPACT_VOLUME_DB: float = 0.0 is a designer-tunable audio-mix value duplicated as a hardcoded const in weapon_audio.gd:21 and projectile.gd:58 (violates designer-first + single-source). 1) In AudioSettings.gd, add to the @export_group("Impact") block (after enemy_hit_pitch_low_hp, ~line 52): `## Volume (dB) an NPC-fired impact one-shot plays at, so 3D distance attenuation applies (the .tscn authors the impact nodes very loud — volume_db 80 — for always-audible PLAYER feedback, which from a distant NPC reads as a flat 2D blast). The player's own shots keep the authored volume.` then `@export var npc_impact_volume_db: float = 0.0`. 2) weapon_audio.gd: delete const NPC_IMPACT_VOLUME_DB (line 21); at line 120 change `NPC_IMPACT_VOLUME_DB if from_ai else source.volume_db` -> `GameSettings.audio.npc_impact_volume_db if from_ai else source.volume_db`. 3) projectile.gd: delete const NPC_IMPACT_VOLUME_DB (line 58); at line 249 change `... else NPC_IMPACT_VOLUME_DB` -> `... else GameSettings.audio.npc_impact_volume_db`. Do NOT edit AudioSettings.tres — a new @export with a default loads as the default from the existing .tres (avoids the import-lock risk on the resource).
- **test:** none: audio-mix value, playtest-only. Optionally add to an AudioSettings contract test (if one exists) `assert_almost_eq(AudioSettings.new().npc_impact_volume_db, 0.0, 0.001)` to pin the default. No behavior change (0.0 == 0.0).
- **docs:** Update the const's old explanatory comments (now on the @export in AudioSettings.gd). Fix the cross-reference comments: weapon_audio.gd:19 ('Mirrors projectile.gd's NPC_IMPACT_VOLUME_DB') and projectile.gd:56-57 — reword to point at GameSettings.audio.npc_impact_volume_db. Add npc_impact_volume_db to the AudioSettings mapping block in GameSettings.gd (lines 73-78) and to docs/AUTHORING_GUIDE.md if it lists AudioSettings knobs.
- **verification:** compile-check · **effort:** S
- **risk:** projectile.gd _emit_impact is a per-hit path, but it ALREADY reads GameSettings.audio.enemy_hit_pitch_*/impact_pitch_* per impact, so one more property read is consistent and cheap (GameSettings.audio is a preloaded resource). Per-frame-autoload reimport-transient canary applies but is pre-existing here. No byte-order/serialization concern.

#### F-C31 · [medium] Probe the Pet/Claim aim ray to the object's slider-max range so a designer max_range above 4 isn't a dead verb
- **files:** scripts/player/pet_interaction.gd, scripts/player/claim_interaction.gd, scripts/components/pettable.gd, scripts/components/claimable.gd
- **change:** PetInteraction/ClaimInteraction cast the aim ray only RAY_REACH=4.0 far, but Pettable.max_range advertises up to 6.0 (@export_range 0.5,6.0) and Claimable.max_range up to 8.0 (@export_range 0.5,8.0), so any max_range in (4, slider-max] is a silently dead verb (the ray never reaches it). The per-object `dist <= max_range` gate remains the real gate, so probing FURTHER is safe (the ray still returns the nearest hit; a too-far object is still rejected by its own gate). Fix = probe to the slider max: pet_interaction.gd:18 change `const RAY_REACH: float = 4.0` -> `const RAY_REACH: float = 6.0` with comment `## probe depth = Pettable.max_range's @export_range max (6.0) so an authored max_range up to the slider ceiling is reachable; the per-object max_range is still the real gate`; claim_interaction.gd:23 -> `const RAY_REACH: float = 8.0` with the analogous comment referencing Claimable.max_range's 8.0 ceiling. Also add a one-line comment on pettable.gd:34 and claimable.gd:47 max_range exports noting the ray probes to this slider's max (keep the two in sync if the ceiling changes) — this makes the slider<->probe contract legible at both ends. Matches the SilentTakedown twin, which already casts the ray to s.max_range (silent_takedown.gd:61).
- **test:** none: playtest-gated (a real aim ray needs the live tree/physics; both drivers run their own _physics_process, off-limits to unit tests per the no-_ready rule). The pure eligibility half is already static/tested; the range/probe is a runtime raycast — verify by aiming at a Pettable authored with max_range 5.5 and confirming the [Q] prompt appears.
- **docs:** The added comments on the RAY_REACH consts and the max_range exports ARE the doc. docs/AUTHORING_GUIDE.md if it documents the Pettable/Claimable max_range field — note the effective ceiling is the slider max.
- **verification:** compile-check · **effort:** S
- **risk:** Casting 6/8m instead of 4m means the ray can now hit a wall/object 4-8m away; _resolve_pettable/_resolve_claimable returns null for a non-target and the per-object gate rejects a too-far target, so no false-positive prompt. Nearest-hit semantics unchanged (a near target is still hit first). Low risk; no shared god-file.

#### F-C62 · [low] Collapse the triplicated explosion reimport-recovery idiom into one Explosion.instantiate_recovering() static
- **files:** scripts/components/explosion_area.gd, scripts/combat/gun_fx.gd, scripts/projectiles/paint_projectile.gd, scripts/effects/explosion.gd, tests/test_effects.gd
- **change:** The explosion-scene reimport-recovery idiom (_explosion_scene + _instantiate_explosion_area) is copy-pasted into gun_fx.gd:21-30, paint_projectile.gd:49-58 (byte-identical), and hand-rolled in explosion.gd:29-32. Add ONE static to explosion_area.gd (class_name Explosion): `const _AREA_SCENE_PATH := "res://scenes/effects/explosion_area.tscn"` and `static func instantiate_recovering() -> Explosion:` that does `var scene := load(_AREA_SCENE_PATH) as PackedScene` (cache hit after first call); if scene==null or not scene.can_instantiate(): `scene = ResourceLoader.load(_AREA_SCENE_PATH, "PackedScene", ResourceLoader.CACHE_MODE_IGNORE) as PackedScene`; if still null/uninstantiable return null; else `return scene.instantiate() as Explosion`. CRITICAL: this script IS explosion_area.tscn's root, so it MUST use runtime load(), NOT preload(its own scene) — that is the documented class_name<->preload(scene) parse cycle. Then rewire callers: gun_fx.gd delete EXPLOSION_AREA_PATH, EXPLOSION_AREA, _explosion_scene(), _instantiate_explosion_area() and replace the 3 `_instantiate_explosion_area()` calls (lines 66/79/92) with `Explosion.instantiate_recovering()`. paint_projectile.gd delete the same 2 consts + 2 statics (lines 15-16, 49-58) and replace the call at line 99 with `Explosion.instantiate_recovering()`. explosion.gd (bridge) delete const EXPLOSION_AREA (line 9) + _explosion_scene() (29-32); in _spawn_at replace `var scene := _explosion_scene(); if scene==null: push_warning(...); return; var explosion = scene.instantiate(); if explosion==null: return` with `var explosion := Explosion.instantiate_recovering(); if explosion == null: push_warning("explosion: explosion_area.tscn unavailable — skipping blast"); return` (the subsequent property sets are valid on the typed Explosion).
- **test:** tests/test_effects.gd: the existing test_explosion_bridge_scene_is_instantiable (lines 36-43) calls the removed bridge._explosion_scene() — REWRITE it to exercise the new source: `var ex := Explosion.instantiate_recovering(); assert_not_null(ex, ...); if ex != null: ex.free()` (bare instantiate does NOT run _ready, so never add_child — free the bare node). Keep it as the regression guard that a destroyed projectile always resolves an instantiable blast.
- **docs:** The recovery-idiom comment now lives once on instantiate_recovering() (fold in the good explanations currently on explosion.gd:25-28 and gun_fx). Note the preload-cycle constraint in that docstring so no future edit reintroduces a preload. scripts/components/README.md if it documents the Explosion component.
- **verification:** gut-editor-closed · **effort:** M
- **risk:** Fallback option if a reviewer insists on keeping a preloaded fast-path: host the static on GunFX instead (class_name GunFX has no scene, so it CAN preload) and route the other two through GunFX.instantiate_explosion_area() — but the review names Explosion.instantiate_recovering(), and runtime load() is cached so the perf delta is negligible. Verify test_effects.gd:37 no longer references _explosion_scene() (it would fail to compile). No hot-path/byte-order concern; cosmetic FX only.

### Batch 17 — T10 physics-layer contract: held-prop mask on talk-LOS + doc correction
**Fixes:** F-C67, F-C68  
**Shared god-files:** scripts/components/ray_cast.gd  
**Why here:** Consistency around the TalkHelpers.held_prop_collision_layer() seam. F-C67 is a comment-only fix (raw bitmask, not layer index) in PhysicsDamageSettings.gd. F-C68 adds the held-prop mask to the talk-LOS occlusion ray + fixes its mis-claim, with an in-tree occlusion test. Re-touches ray_cast.gd (distinct method from Batch 9's T2-1 edit). No deps.  
**Verify:** NOW: compile via -s. DEFERRED: the in-tree two-physics-frame occlusion test (held-prop between doesn't occlude) is GUT editor-closed; behaviorally inert in normal play.

#### F-C67 · [low] Correct pickup_held_collision_layer doc from "layer index" to raw bitmask
- **files:** resources/tuning/PhysicsDamageSettings.gd
- **change:** At line 61, the @export doc comment for `pickup_held_collision_layer: int = 4` currently ends `...(so it stops colliding with the world/player). A layer index.` — this is factually inverted. Replace the trailing sentence so it reads as a RAW collision bitmask, e.g.: `## Physics collision layer a held object is moved to while carried (so it stops colliding with the world/player). RAW collision-layer BITMASK, not a 1<<index: PickupRay does collision_layer = this directly and every sight/perception ray clears it via TalkHelpers.held_prop_collision_layer() & ~mask. Value 4 = editor layer 3.` Comment text only; do NOT touch the `= 4` value, the field name, or any other line. Explicitly do NOT rename to pickup_held_collision_layer_mask.
- **test:** none: doc-only reword, no behaviour change. The raw-bitmask contract is already pinned by tests/test_held_prop_los.gd:19-22 (helper == tuning value, >0) — that test keeps passing unchanged and is the regression lock the reworded prose now matches.
- **docs:** The edited line IS the authoring doc (Inspector tooltip). Cross-check: scripts/dialogue/talk_helpers.gd:17-27 already states the raw-bitmask rule correctly — no edit needed there; this fix brings the export's prose into agreement with it. docs impact: none beyond the comment.
- **verification:** code-only · **effort:** S
- **risk:** None functional — comment-only. The tempting-but-rejected alternative (rename field to _mask) is byte-fragile here: it would touch ray_cast.gd:396, the &"pickup_held_collision_layer" string read in talk_helpers.gd:32, the editor-LOCKED PhysicsDamageSettings.tres resource value, and tests test_held_prop_los.gd + test_managers_tuning.gd; the .tres could silently revert while the editor is open. Not a god-file.

#### F-C68 · [low] Add held-prop mask to talk-LOS occlusion ray + fix its "mirrors Pet/Claim" comment
- **files:** scripts/components/ray_cast.gd, tests/test_interaction_occlusion.gd
- **change:** Two edits in ray_cast.gd, both inside _interaction_occluded(): (1) Line 349, change `q.collision_mask = 0xFFFFFFFF & ~TalkHelpers.TALK_LAYER` to `q.collision_mask = 0xFFFFFFFF & ~TalkHelpers.TALK_LAYER & ~TalkHelpers.held_prop_collision_layer()` — matching the exact mask composition already used in pet_interaction.gd:70 and claim_interaction.gd:120. (2) Update the doc comment at lines 335-336 so the parenthetical no longer claims the mask is `0xFFFFFFFF & ~TALK_LAYER`: change it to state it mirrors PetInteraction/ClaimInteraction's FULL mask `0xFFFFFFFF & ~TALK_LAYER & ~held_prop_collision_layer()` — clearing both the talk layer AND the held-prop bit so a prop carried in front of the camera can never occlude a look-at verb (defense-in-depth; today unreachable while carrying but keeps this chokepoint semantically identical to its five sibling sight rays). Keep the existing note that line-of-FIRE rays deliberately do NOT clear the held bit.
- **test:** Add one in-tree physics test to tests/test_interaction_occlusion.gd, mirroring the existing harness (reuse _make_ray/_make_pickup and the two-physics-frame await): `func test_held_prop_between_does_not_occlude_the_pickup() -> void:` — build the ray, a pickup at (0,0,-2), then a StaticBody sized like _make_wall but on layer `GameSettings.physics_damage.pickup_held_collision_layer` at (0,0,-1); after two physics frames assert `ray._query_talk_handler() == cp` (a carried-prop-layer body between camera and target must NOT occlude, matching test_held_prop_los.gd's sight case). Add a tiny local helper or parametrize _make_wall to accept a layer arg (default 1) so the existing wall tests are unchanged. Uses assert_eq only (no assert_le/ge); does NOT run Player/NPC _ready — PickupRay.new()+add_child with set_physics_process(false), the pattern already in this file.
- **docs:** Comment at ray_cast.gd:335-336 is the doc surface and is corrected as part of change (2). No CURRENT_ARCHITECTURE/AUTHORING_GUIDE change: the held-prop-sight-transparent contract is already documented via talk_helpers.gd. docs impact: none beyond the in-file comment.
- **verification:** compile-check · **effort:** S
- **risk:** Behaviourally inert in normal play (you cannot trigger a look-at talk/pickup while carrying a prop, so the held bit is never an occluder candidate today) — this is consistency + defense-in-depth. The extra `& ~...()` is a trivial bit-op, not a byte-order/hot-path concern even though the occlusion ray runs per interaction frame. ray_cast.gd (PickupRay) is widely used but is NOT one of the shared god-files (npc.gd/player.gd/GameState.gd/InputManager.gd); the edit is scoped to one method. Only risk is over-masking if some genuine WALL were ever authored on the held-prop layer (4) — but layer 4 is reserved exclusively for carried props by this same contract, so that cannot legitimately happen.

### Batch 18 — T11 new-system polish: refuse-emits-closed, player-menu bail, input gates, FX/AI knobs
**Fixes:** F-C47, F-C39, F-C38, F-C41, F-C49, F-C50, F-C51, F-C46  
**Shared god-files:** scripts/player/player.gd, scripts/ui/hotbar.gd, scripts/components/chip_installer.gd  
**Why here:** F-C39 first and paired with its test rewrite — it breaks test_player_menus::test_any_open_and_close_others (which exploits ReputationScreen as the one player-free-openable screen); the collision must land in the same diff. F-C47 (_refuse_open emits closed on every guard), F-C38 (F5/F9 gameplay_suppressed gate; re-touches player.gd), F-C41 (hotbar NameEntry gate), F-C49 (PS1 skip non-BaseMaterial3D), F-C50 (HALF_PAWN const), F-C51 (drop 0-value chips), F-C46 (freed-capture lambda→bound method) are independent S edits.  
**Verify:** NOW: compile via -s; off-tree/seam tests (refuse emits closed, has_player false without player + rewritten sibling test, zero-value chip filtered, ps1 skips ShaderMaterial, HALF_PAWN==50, music-pick survives freed row). DEFERRED: F5/F9 + d-pad + PS1 render behaviors playtest; new test sidecars editor-closed.

#### F-C47 · [critical] Route every Shop/ChipInstall/Chess refuse path through a `_refuse_open()` that emits `closed`
- **files:** scripts/ui/shop_screen.gd, scripts/ui/chip_install_screen.gd, scripts/ui/chess_screen.gd
- **change:** The dialogue-suspend seam (scripts/dialogue/dialogue_manager.gd:297 `_suspend_for_menu`) sets `_suspended=true`, connects the screen's `closed` as a CONNECT_ONE_SHOT to `_resume_from_menu`, THEN calls the open. Any early-return in the open that does NOT emit `closed` strands the conversation `_suspended` forever (box hidden, tree paused, is_active()==false). Add to EACH of the three screens a private helper:

	func _refuse_open() -> void:
		# Guard failed: we never opened, but a dialogue-hosted open suspended the conversation on our `closed`
		# one-shot BEFORE calling us. Emit `closed` so _resume_from_menu re-shows the box; on the standalone
		# path nothing is listening, so it is harmless. Do NOT touch pause/mouse/_is_open here (never mutated yet).
		closed.emit()

Then convert EVERY guard early-return in the open method to `_refuse_open(); return`:
- shop_screen.gd open_shop: the `_is_open or DialogueManager.is_active() or InputManager.any_modal_open(self)` return (~L45-46); the `not is_instance_valid(merchant)` return (~L49-50); the `not (stock_v is CharacterInventory)` return (~L52-53); the `not is_instance_valid(_player) or _player.inventory == null` return (~L55-56).
- chip_install_screen.gd open_install: the L41-42 modal/dialogue guard; the L45-46 `not has_method(&"install_carried")` guard; the L48-49 no-player/inventory guard.
- chess_screen.gd open_match: the L71-72 modal/dialogue guard; the L75-76 invalid-match guard; the L78-79 no-player guard. The L86-95 wager-cannot-cover path already emits `closed` manually — keep the toast + `_match=null`/`_player=null` clears but replace its bare `closed.emit()` (L94) with `_refuse_open()` for uniformity. NOTE: chess already documents this contract on that one path (L91-94); its comment is the template for the helper doc.
- **test:** tests/test_chess_ai.gd is off-tree logic only — add screen-level closed-on-refuse coverage to a NEW tests/test_dialogue_suspend_closed.gd or extend an existing in-tree screen test: for each of ShopScreen/ChipInstallScreen/ChessScreen, connect a one-shot flag to `closed`, call the open method with an INVALID node arg (e.g. `Node.new()` lacking the duck-typed surface) and a null player, assert `is_open()==false` AND the `closed` flag fired. Use assert_true/assert_false only. Do NOT build a real Player (call open with `null` player — the invalid-node guard fires first). Autofree the throwaway Node.
- **docs:** Update each screen's open-method docstring to state 'every refuse path emits closed (dialogue-suspend contract) via _refuse_open'. Add a one-line note to docs/CURRENT_ARCHITECTURE.md dialogue-suspend section that suspending sub-menus MUST emit closed on refuse.
- **verification:** compile-check · **effort:** M
- **risk:** Edge case: routing the `_is_open`-already-open guard through _refuse_open would emit `closed` while the screen is genuinely open — but this is unreachable from the suspend seam (tree is paused, no second conversation can drive a re-open), and DialogueManager.is_active() reads false while suspended so that clause won't trip from the hosting dialogue. Standalone (Merchant/ChipInstaller/ChessMatch.start_talk) opens have no `closed` listener, so a refuse-emit is a no-op there. No hot path.

#### F-C39 · [high] QuestJournal (J) + ReputationScreen (V) must bail when there is no human player
- **files:** scripts/ui/quest_journal.gd, scripts/ui/reputation_screen.gd, scripts/ui/player_menus.gd, tests/test_player_menus.gd
- **change:** Both screens are read-only globals (quest log reads GameState, rep reads Reputation autoload) and never resolved a `_player`, so unlike InventoryScreen/StatsScreen (which call `_find_real_player()` and bail on invalid) they open over the start menu / character creation. player_menus.gd already documents (test_obj_alive_refuses_only_a_dead_player) that 'the screen's own null-check owns no-player-to-show' — these two screens lack it. Fix:
1) Add a static helper to scripts/ui/player_menus.gd next to player_alive:
	static func has_player(tree: SceneTree) -> bool:
		return Groups.human_player(tree) != null  # the human Player exists in-tree (start menu / char-creation have none)
(player_menus.gd is const-preloaded, class-free — Groups is a global class_name util, safe to reference.)
2) In quest_journal.gd open() (~L35-38) and reputation_screen.gd open() (~L45-48), add `or not PlayerMenus.has_player(get_tree())` to the existing refuse condition (alongside the DialogueManager/OptionsMenu/LootScreen/any_pausing_open/player_alive clauses). Add a trailing comment: '# no human player (start menu / char-creation) -> nothing to show, matching Inventory/Stats'.
- **test:** In tests/test_player_menus.gd: (a) add test_has_player_false_without_a_player -> assert_false(PM.has_player(get_tree())) (GUT scene has no Player). (b) MANDATORY: fix the existing test_any_open_and_close_others_switch_off_a_sibling (L37-44) which today calls `ReputationScreen.open()` and relies on it opening WITHOUT a player — after this fix that open is refused. Rewrite it to the new contract: assert ReputationScreen.open() with no player leaves any_open()==false, and assert QuestJournal.open() likewise stays closed; verify close_others is a no-op when nothing is open. Note in the test comment that the live open->sibling-switch is playtest-verified (in-tree convention; a real Player cannot be add_child'd in a unit test per CLAUDE.md).
- **docs:** Update the open() docstrings in both screens to state they refuse with no human player. No architecture-doc change (behavior now matches the already-documented Inventory/Stats contract).
- **verification:** compile-check · **effort:** M
- **risk:** CROSS-TEST COLLISION: test_player_menus.gd::test_any_open_and_close_others_switch_off_a_sibling currently exploits ReputationScreen as the one player-free-openable screen; it WILL fail unless rewritten in the same diff. Cannot substitute a real in-tree Player (Player._ready forbidden in unit tests). player_menus.gd is shared by all four Pip-Boy screens — the has_player helper is additive (no behavior change to Inventory/Stats).

#### F-C38 · [high] Gate F5/F9 quicksave/quickload on gameplay_suppressed() so they can't fire under non-pausing overlays
- **files:** scripts/player/player.gd
- **change:** player.gd `_update_save_input()` (L1480, called from _physics_process L1747) only escapes during dialogue (the L1735 early-return) and while the tree is paused (transaction screens). Non-pausing overlays (Inventory/Stats/Reputation/Journal/Options/Loot/name-entry) do NOT pause, so F5/F9 still fire — quickload then reloads the scene UNDER an open menu. Fix: add an early guard as the first line of `_update_save_input()`:
	if InputManager.gameplay_suppressed():
		return  # a non-pausing overlay (inventory/stats/loot/options/name-entry) or cutscene is up: no quick-save/load under it
gameplay_suppressed() (InputManager.gd:100) already unions every overlay INCLUDING NameEntryDialog and CutscenePlayer, so this one guard covers all cases. Update the L1475-1479 doc comment: replace 'suppressed during a conversation ... and while the tree is paused' with a note that it now also refuses under any non-pausing overlay via gameplay_suppressed().
- **test:** none: _update_save_input reads live autoload state (InputManager.gameplay_suppressed) + Input polling; it cannot be exercised off-tree without running Player._ready (forbidden). Behavior is playtest-gated (open Inventory, press F9, confirm no reload).
- **docs:** Fix the nearby comment block at player.gd:1475-1479 to state the new gameplay_suppressed gate.
- **verification:** compile-check · **effort:** S
- **risk:** player.gd is a shared god-file touched by multiple themes — keep the edit to the two lines inside _update_save_input; do not disturb _physics_process ordering. gameplay_suppressed() is polled each frame here but only when F5/F9 is pressed (Input.is_action_just_pressed short-circuits after), so no hot-path cost.

#### F-C41 · [medium] Gate the hotbar against controller d-pad input while the pet-naming dialog is open
- **files:** scripts/ui/hotbar.gd
- **change:** hotbar.gd `_unhandled_input` gate (L134-136) routes through `InputManager.any_modal_open(InventoryScreen)`, but NameEntryDialog is deliberately EXCLUDED from InputManager._modal_screens() (it suppresses control, isn't a stackable menu). A focused LineEdit doesn't consume joypad events, so the d-pad still equips weapons / eats medkits during pet-naming. Fix: add `or NameEntryDialog.is_open()` to the gate:
	if _player == null or _player._dead or DialogueManager.is_active() \
			or NameEntryDialog.is_open() \
			or InputManager.any_modal_open(InventoryScreen):
		return
Add to the existing gate comment (L128-133) that NameEntryDialog is gated explicitly because it is excluded from the modal set yet must still swallow hotbar input.
- **test:** none: _unhandled_input reads the live NameEntryDialog autoload + player state and can't run without an in-tree Player. Playtest-gated (open the pet-name dialog, press d-pad, confirm no weapon swap). Optionally assert in tests/test_hotbar.gd that the gate's dependency exists — skip unless a bare-hotbar harness already stubs these autoloads.
- **docs:** Update the inline gate comment in hotbar.gd only.
- **verification:** compile-check · **effort:** S
- **risk:** NameEntryDialog is an autoload with is_open() (confirmed). gameplay_suppressed() already includes NameEntryDialog, but the hotbar cannot use gameplay_suppressed() wholesale because it needs the InventoryScreen exclusion (backpack-open assigns to a slot) — hence the targeted addition. Low risk.

#### F-C49 · [high] PS1 warp: skip non-BaseMaterial3D (ShaderMaterial / null) surfaces instead of painting them flat white
- **files:** scripts/effects/ps1_applier.gd
- **change:** `_ps1ify` (L132) now runs globally under every LevelRoot via the Ps1Warp autoload with no idempotence guard. The surface loop only special-cases transparent BaseMaterial3D (L140); a ShaderMaterial (or a null/no-material surface) falls through and is replaced with the opaque warp shader using tex=null/col=WHITE (L146-153) -> flat opaque white, silently destroying any authored ShaderMaterial and giving no idempotence on a re-walk. Fix: at the top of the per-surface body (right after `var src := mi.get_active_material(s)`, before the transparency check), add:
	# Only warp plain BaseMaterial3D surfaces — we carry their albedo tex/colour into the warp. A ShaderMaterial
	# (including our OWN ps1 override on a re-walk -> idempotent) or a null/None surface has no albedo to read;
	# swapping it for the opaque warp shader would paint it flat white and wipe an authored shader effect. Skip it.
	if not (src is BaseMaterial3D):
		continue
Then the existing transparency guard becomes `if (src as BaseMaterial3D).transparency != BaseMaterial3D.TRANSPARENCY_DISABLED: continue` and the later `if src is BaseMaterial3D:` branch at L148 is now always true — simplify it to read tex/col directly from `(src as BaseMaterial3D)` (drop the now-dead else-white fallback) so the comment stays true. Update the top-of-file 'Skips:' doc (L19-21) to add: skips ShaderMaterial / material-less surfaces (only BaseMaterial3D is warpable).
- **test:** tests/ has no ps1 test — add tests/test_ps1_applier.gd (gut-editor-closed). Build the applier off-tree: `var a = load("res://scripts/effects/ps1_applier.gd").new()` (no add_child, so _ready never runs). Make a MeshInstance3D with a 1-surface ArrayMesh whose surface material is a ShaderMaterial; call `a._ps1ify(mi)`; assert `mi.get_surface_override_material(0) == null` (skipped) and `a._warped.is_empty()`. Second case: a BaseMaterial3D opaque surface -> assert override IS set and is a ShaderMaterial. Free mi and a. Use assert_null/assert_not_null/assert_true only.
- **docs:** Update the ps1_applier.gd header 'Skips:' block; no other doc references the internal branch.
- **verification:** compile-check · **effort:** S
- **risk:** Byte-order/render hot path: _ps1ify runs during the level walk, not per-frame, so cost is one-time. Risk is visual: verify (editor-closed playtest) that opaque textured surfaces still warp and that glass/foliage + any ShaderMaterial props render normally. The idempotence guard (ShaderMaterial skip) also protects a defensive re-walk.

#### F-C50 · [medium] Chess AI tie-window: 0.5 -> a named HALF_PAWN := 50.0 const (half a pawn, not 1/200th)
- **files:** scripts/chess/chess_ai.gd
- **change:** Piece values are centipawns (VALUES[PAWN]=100, L14). The move-variety tie group at L119 keeps moves within `best - float(e.s) <= 0.5` — 0.5 centipawns, effectively a deterministic argmax, so matches vs the same NPC replay near-identically (the L106-107 comment promises 'within ~half a pawn'). Fix: add near L18 `const HALF_PAWN := 50.0  ## tie-group width: keep every root move within half a pawn (50 cp) of best, then pick at random for cross-game variety`, and change L119 to `if best - float(e.s) <= HALF_PAWN:`.
- **test:** In tests/test_chess_ai.gd add test_half_pawn_tie_window_is_half_a_pawn: assert_eq(ChessAiScript.HALF_PAWN, 50.0, 'tie window is half a pawn in centipawns, not ~0'); assert_true(ChessAiScript.HALF_PAWN == ChessAiScript.VALUES[1] / 2.0, 'stays half of a pawn if the pawn value changes'). (Behavioral variety across games is playtest-observed; the const pin guards the regression.)
- **docs:** None beyond the inline const comment (the L106-107 comment already describes half-a-pawn correctly).
- **verification:** compile-check · **effort:** S
- **risk:** Pure logic, off-tree; no hot-path or shared-file concern. Only affects opponent move variety, never legality/mate-finding (those tests still pass — a wider tie window only groups near-equal moves).

#### F-C51 · [medium] ChipInstaller: exclude 0-value chips from the installable lists (no permanently-disabled '0 zm' row)
- **files:** scripts/components/chip_installer.gd
- **change:** A chip with `value <= 0.0` passes is_upgrade_chip() so it enters `_distinct_installable` (L129) and renders in ChipInstallScreen._fill/_make_row (chip_install_screen.gd L142-144) at price 0 -> `affordable = price > 0 and ...` is false -> a disabled '0 zm' row, contradicting install_fee's contract comment (chip_installer.gd L90-91: 'the install screen won't offer those'). Fix the shared filter at chip_installer.gd L134: change `if item == null or not item.is_upgrade_chip():` to `if item == null or not item.is_upgrade_chip() or item.value <= 0.0:`. Both install_fee (L93) and buy_and_install_cost (L100) already return 0 for value<=0, so one filter edit covers BOTH the carried and stock sections. Add a comment: '# value<=0 chip has a 0 fee (install_fee returns 0) -> would render a permanently-disabled row; drop it here so the contract holds.'
- **test:** In tests/test_chip_install.gd (off-tree ChipInstaller.new(), _chip helper already supports value): add test_zero_value_chip_is_not_installable — put a `_chip(&"grapple", 0.0)` in the player's inventory, assert `installer.installable_carried(player).is_empty()`; and a value>0 chip is present. Also assert a 0-value chip in `installer.stock` is absent from `installable_stock(player)`. Use assert_true(arr.is_empty()) / assert_eq(arr.size(), 1). Follow the file's _installer/_player/_teardown helpers.
- **docs:** None (install_fee's existing comment becomes true; add the inline filter comment).
- **verification:** compile-check · **effort:** S
- **risk:** Low. Shared filter is the single chokepoint for both sections. A legitimately free chip (value 0) is arguably a data error anyway; excluding it is the documented intent.

#### F-C46 · [medium] Music-folder FileDialog: replace freed-capture lambdas with bound guarded methods
- **files:** scripts/ui/options_menu.gd
- **change:** `_open_music_folder_dialog` (L377) connects two LAMBDAS that CAPTURE `path_btn` (a row Button that _rebuild_tabs frees) and `dlg`. Per the project's 'lambda freed-capture guard is useless' rule, a freed capture errors BEFORE the lambda body runs, so a pick that lands after a tab rebuild silently loses the setting AND leaks the dialog. `dlg` is add_child'd to OptionsMenu (self, persistent), so only `path_btn` can be freed. Fix: replace the two `.connect(func...)` with bound METHOD Callables (bound args arrive as method params, guardable with is_instance_valid):
	dlg.dir_selected.connect(_on_music_folder_picked.bind(dlg, path_btn))
	dlg.canceled.connect(_free_dialog.bind(dlg))
and add:
	func _on_music_folder_picked(d: String, dlg: FileDialog, path_btn: Button) -> void:
		Settings.set_music_folder(d)                 # persist FIRST — the pick must survive a freed row button
		if is_instance_valid(path_btn):
			path_btn.text = _music_folder_label()   # refresh the caption only if the row still exists
		_free_dialog(dlg)
	func _free_dialog(dlg: FileDialog) -> void:
		if is_instance_valid(dlg):
			dlg.queue_free()                         # one-shot dialog: always freed on select OR cancel
(dir_selected emits `d`; bound args append AFTER, so the param order is d, dlg, path_btn.) Update the L375-376 comment to note the guarded-method binding (not a lambda) is why the pick survives a rebuilt tab.
- **test:** In tests/test_options_menu.gd (uses the live OptionsMenu autoload): add test_music_folder_pick_survives_a_freed_row_button. Save `var prev := Settings.music_folder`. Make a throwaway Button, free() it, then call `OptionsMenu._on_music_folder_picked("res://", null, freed_btn)` — assert no crash and `Settings.music_folder == "res://"`. Restore `Settings.set_music_folder(prev)` in the test (and/or after_each). Pass null for dlg to avoid a real FileDialog (the is_instance_valid(dlg) guard no-ops). Use assert_eq/assert_true.
- **docs:** Update the inline comment in options_menu.gd; note the pattern reference (memory: bound method Callable, not lambda, for freed-capture safety).
- **verification:** compile-check · **effort:** S
- **risk:** Settings.set_music_folder persists config to disk — the test writes then restores the prior value; acceptable in the test env but must restore. options_menu.gd is not a god-file. Verify (playtest) that a normal in-place pick still updates the button caption.

### Batch 19 — REM code + remediation-status reconciliation
**Fixes:** F-REM-C59, F-REM-C54, F-REM-DOCS  
**Why here:** F-REM-C59 (two-word stale comment) and F-REM-C54 (ragdoll NodeFinder + null-guard + scene-drift test) are independent concrete fixes. F-REM-DOCS depends on both C54 and C56 (Batch 2) so its backlog note records B-F19/B-F63 as freshly resolved; it stamps DONE/OPEN markers on the 2026-07-01 plan, mirrors the 6-item backlog into CURRENT_ARCHITECTURE, and refreshes the ~/.claude memory entry (outside the git repo, never touches the dirty tree).  
**Verify:** NOW: compile via -s (ragdoll.gd). DEFERRED: test_ragdoll_scene.gd GUT run + its .gd.uid sidecar editor-closed; F-REM-DOCS is doc-only (no compile). Confirm C59 comment persisted (re-read; buffered-revert).

#### F-REM-C59 · [low] Fix stale chip-count in test_chip_install.gd comments (says 6, asserts 7)
- **files:** tests/test_chip_install.gd
- **change:** Two comment-only edits to match the code, which already asserts 7 (line 262: `assert_eq(count, 7, ..."incl. the Board Visualizer")`). (1) Line 7: change `Also pins the 6 authored chip .tres + the mechanic scene.` -> `Also pins the 7 authored chip .tres + the mechanic scene.` (2) Line 238 section-banner comment `# --- The authored content: 6 chip .tres + the reusable mechanic scene ...` -> `7 chip .tres`. Do NOT touch any assert or its message — the 7 is correct (one chip per shipped ability, including the Board Visualizer added by the blindfold-chess feature). No logic change.
- **test:** none: the affected file IS the test; the assertion already at line 262 (`assert_eq(count, 7 ...)`) is the contract and stays unchanged. Nothing to add.
- **docs:** none (the edited lines are the doc/comment)
- **verification:** code-only · **effort:** S
- **risk:** Editor is OPEN and may have this .gd buffered (writes-can-revert). After editing, re-Read lines 7 and 238 to confirm the change stuck. Pure comment text — no compile impact, cannot break the suite.

#### F-REM-C54 · [medium] Ragdoll corpse light: replace brittle deep NodePath with NodeFinder lookup + null-guard the fade deref, add scene-drift test (B-F19)
- **files:** scripts/components/ragdoll.gd, tests/test_ragdoll_scene.gd, scripts/components/README.md
- **change:** ragdoll.gd currently binds the corpse light via a ~15-segment `@onready var corpse_light: OmniLight3D = $"Sketchfab_Scene/.../OmniLight3D"` (line 4). Any re-import or re-rig of the skeleton GLB silently makes that path resolve to null, and `_process` (line 51) then hard-crashes on `corpse_light.omni_range`. Fix, mirroring the in-file NodeFinder idiom already used for the skeleton at line 129:
(1) Line 4 -> `@onready var corpse_light: OmniLight3D = NodeFinder.find_first_of_class(self, OmniLight3D) as OmniLight3D`  (add a one-line comment: the deep authored NodePath was drift-fragile; find the first OmniLight3D under the ragdoll root instead).
(2) In `_process` (lines 47-51) add a null guard BEFORE the deref so a scene without the light degrades gracefully instead of crashing:
```
func _process(delta: float) -> void:
	if !_fading:
		return
	if corpse_light == null:
		return  # scene drift: no OmniLight3D found — skip the fade-out dimming rather than crash
	var t := 1.0 - exp(-fade_speed * delta)
	corpse_light.omni_range = lerpf(corpse_light.omni_range, 0.0, t)
```
Use TABS. No behavior change when the light is present. The ragdoll scene is `scenes/props/skeleton.tscn` (ext_resource id 1_rwpf0 = ragdoll.gd; root `Sketchfab_Scene`; carries an OmniLight3D + PhysicalBoneSimulator3D — grep-confirmed).
- **test:** Create tests/test_ragdoll_scene.gd (new GUT test, scene-drift contract):
```
extends GutTest
const RAGDOLL_SCENE := "res://scenes/props/skeleton.tscn"
func test_ragdoll_scene_carries_corpse_light_and_bones() -> void:
	var ps := load(RAGDOLL_SCENE) as PackedScene
	assert_not_null(ps, "skeleton.tscn (the ragdoll scene) must load")
	var root := ps.instantiate()  # off-tree instantiate does NOT run Ragdoll._ready (no physics await) — safe per the no-_ready rule
	assert_not_null(NodeFinder.find_first_of_class(root, OmniLight3D), "ragdoll scene must carry an OmniLight3D corpse light — Ragdoll._process fades it; drift guard")
	assert_not_null(NodeFinder.find_first_of_class(root, PhysicalBoneSimulator3D), "ragdoll scene must carry a PhysicalBoneSimulator3D so the corpse can ragdoll")
	root.free()
```
Respects the no-_ready rule (instantiate() off-tree never fires _ready), no assert_le/ge used. This test is what catches the exact drift the deep NodePath silently swallowed.
- **docs:** scripts/components/README.md — update the Ragdoll entry to state the corpse light is now discovered via NodeFinder.find_first_of_class(self, OmniLight3D) (not a hardcoded Sketchfab NodePath), and that test_ragdoll_scene.gd pins the scene's OmniLight3D + PhysicalBoneSimulator3D against re-import drift. Also refresh the nearby line-4 code comment if one exists.
- **verification:** gut-editor-closed · **effort:** M
- **risk:** ragdoll.gd is a small isolated component (not a god-file) — low blast radius. Editor OPEN: the .gd edit compiles via headless `-s` now (compilation only), but the new test + the runtime behavior are GUT/playtest work for an editor-CLOSED session. New tests/test_ragdoll_scene.gd needs a .gd.uid sidecar — it will be generated on the next `--import` when the editor is available; do not hand-write the uid. NodeFinder is a global class_name (no import needed in the test).

#### F-REM-DOCS · [low] Reconcile the 2026-07-01 remediation plan with reality: park M1, mark M13 PARTIAL, add DONE markers, refresh memory (C53/M1 + C55 + C57/M13)
- **files:** docs/audits/architecture-remediation-plan-2026-07-01.md, docs/CURRENT_ARCHITECTURE.md
- **change:** The nine-wave plan is ~85% executed but carries ZERO completion markers, so it now materially misstates what is done. Three doc-only reconciliations, all against the same plan doc plus a short open-backlog note in CURRENT_ARCHITECTURE:
(1) C55 — Add per-item status markers to the plan. Prefix each item verified-done in the review's 'Verified DONE' list (H1, H1b, M3, PL1-PL6, H3, M4, M5, M6, M9, M2, M7, M8, M14, T1, H2, H2b, XC1, M11, M15, B-F40, B-F62, B-F61, B-F7) with `[DONE]`. Leave the ~6 open items (C22/B-F24, C53/M1, C57/M13, C54/B-F19, C56/B-F63, C58/M5) marked `[OPEN]`.
(2) C57/M13 — On the M13 row (line 184) and the M13 note (line 194) mark `[PARTIAL]`: `scripts/player/ground_movement.gd` shipped (statics only), but `scripts/player/landing.gd` was NEVER created (glob-confirmed absent) — the Landing/footstep component extraction is outstanding. State the safe descope already noted in the plan (GroundMovement.target_speed alone was the shipped sub-win).
(3) C53/M1 — Record the scheduling DECISION on the M1 row (line 204) and the caveat (line 210): FORMALLY PARK M1. Rationale to write: the trigger (next project.godot edit) has fired 3x without riding along, the tree is heavily dirty (project.godot likely mid-edit), and M1 is an L-effort autoload-ordering change that must be a single atomic commit — doing it opportunistically now is unsafe. M15 and B-F40 already landed cleanly inside the current GameState, so the split has no functional blocker; treat M1 as a standalone future commit, NOT abandoned. Do NOT perform the extraction in this pass.
Then add a ~5-line 'Remediation backlog (as of 2026-07-12)' note to docs/CURRENT_ARCHITECTURE.md listing the 6 still-open items with one-line status each, so the live architecture doc (not just an audit file) reflects the truth.
- **test:** none: doc-only, no runtime or scene surface.
- **docs:** This fix IS the doc work: docs/audits/architecture-remediation-plan-2026-07-01.md (status markers) + docs/CURRENT_ARCHITECTURE.md (open-backlog note). PLUS refresh the standing memory entry C:/Users/dalla/.claude/projects/C--Users-dalla-3D-RPG/memory/arch-review-and-remediation-2026-07-01.md and its one-line summary in MEMORY.md — change 'PLANNED not implemented; do-first = H1 atomic autosave' to reflect ~85% DONE with the 6-item open remainder (M1 parked, M13 partial, B-F19/B-F63 addressed by this batch).
- **depends_on:** F-REM-C54, F-REM-C56
- **verification:** code-only · **effort:** M
- **risk:** Doc-and-memory only; no code, no compile, no test impact — cannot break the build. Only risk is prose drift: keep the DONE/OPEN lists exactly matching the review's verified lists (don't invent statuses). Do NOT delete the plan file — annotate it (CLAUDE.md docs-hygiene prefers current-only, but the review explicitly offered annotate-OR-delete; annotate is safer since other audit docs cross-reference wave IDs). The memory file lives OUTSIDE the git repo (under ~/.claude) so it never touches the dirty tree.

### Batch 20 — HYGIENE doc-sync: architecture/authoring/refuted breadcrumbs
**Fixes:** F-HYG-2, F-HYG-3, F-HYG-4  
**Shared god-files:** scripts/ui/hotbar.gd  
**Why here:** Doc-sync lands after all code fixes so prose never precedes reality (stale docs are worse than none). F-HYG-2/3 depend on F-HYG-6's staging rules and reconcile CURRENT_ARCHITECTURE (save model, SAVE_VERSION=2, cutscene freeze, modal registry) + AUTHORING_GUIDE (new exports). F-HYG-4 adds the single consolidated 'reviewed & intentionally-not-a-bug' ledger (avoids scattering comments into the shared god-files themes 2/3/7/8 rewrote). Each doc claim now matches an already-landed code fix.  
**Verify:** code-only (Markdown + one hotbar.gd comment). rg CURRENT_ARCHITECTURE/AUTHORING_GUIDE for stale twins ('not yet gated on', 'DERIVED from bag contents', 'pickup_held') after editing.

#### F-HYG-2 · [high] CURRENT_ARCHITECTURE.md save-model / contract sync (hotbar, SAVE_VERSION=2, cutscene pause, modal registry)
- **files:** docs/CURRENT_ARCHITECTURE.md, scripts/ui/hotbar.gd
- **change:** Four coordinated edits, each landing in the SAME diff as its code fix (edit the doc only once the corresponding code fix is in the working tree): (a) HOTBAR SAVE [C42] — the Save Model section currently omits hotbar layout, and scripts/ui/hotbar.gd:18 comment falsely claims 'assignment is DERIVED from bag contents in insertion order, so a saved game rebuilds the same layout.' When C42 serializes the bar, add a sentence to the Save Model list (line 40-44 enumeration) that hotbar slot assignments now persist as a `[hotbar]` section, and REWRITE the hotbar.gd:18 comment to state the real contract (layout is serialized, not re-derived). If C42 instead lands as 'derive deterministically', correct the comment to say why manual holdable assignments survive. (b) SAVE_VERSION=2 [C43] — replace the 'Every save stamps [meta].version (SAVE_VERSION) — read into save_version for a future migration, not yet gated on.' clause (lines 95-96) with prose stating SAVE_VERSION is now 2, the load path folds legacy `persuasion` points into `streetwise` for save_version<2, and this is the first live use of the H1b version seam. (c) CUTSCENE PAUSE [C34] — in Run And Level Flow (the cutscene mention at line ~32) add that cutscenes now freeze world side-effects (either get_tree().paused with the Player kept ALWAYS, or a shared world_frozen() helper), so hazard/DoT/NPC-fire no longer hit a control-locked player. (d) MODAL REGISTRY [Theme 1] — add a short paragraph (Components or Testing Strategy section) documenting InputManager._modal_screens() as the SINGLE registry consumed by suppression, pause-query, death-close (Player._close_open_modals), and per-screen open-gates, pinned by the drift test — so future screens register in one place.
- **test:** none: doc + comment change, no runtime surface. (C42's own round-trip test lives in its finding; a save_version<2 fold test lives in F-HYG-1's test_save_migration_v2.gd.)
- **docs:** This finding IS the doc update. Also `rg` CURRENT_ARCHITECTURE.md for 'not yet gated on' and 'DERIVED from bag contents' after editing to confirm no stale twin remains.
- **depends_on:** F-HYG-6
- **verification:** code-only · **effort:** M
- **risk:** Doc prose must not land before its code fix or it will misstate reality (worse than silence per CLAUDE.md docs-hygiene). hotbar.gd is a shared file also touched by C11/C42/C13/C28 — coordinate: apply the comment edit in the same pass as C42's serialization and re-read before editing (writes-can-revert). Do not claim the profile save became an exact snapshot — [hotbar] is additive profile data.

#### F-HYG-3 · [medium] AUTHORING_GUIDE.md sync for new/changed designer-facing exports
- **files:** docs/AUTHORING_GUIDE.md
- **change:** Document the exports this batch adds or reshapes, so the editor (the modding surface) stays truthful: (a) C63 — when NPC_IMPACT_VOLUME_DB moves from a hardcoded const into the audio tuning group (resources/tuning/NpcAudioSettings.gd or AudioSettings.gd), add a line under the weapon-Audio treatment near AUTHORING_GUIDE.md:1501 naming the new tunable field and that it lives on the GameSettings audio resource, not in code. (b) C67 — the held-prop contract: pickup_held_collision_layer export doc is being reworded (value 4 = editor layer 3, a MASK, possibly renamed *_mask); if the field is renamed, `rg` AUTHORING_GUIDE.md + CURRENT_ARCHITECTURE.md for 'pickup_held' and update every mention to the new name and the mask semantics. (c) C51 — install_fee<=0 rows are now filtered from ChipInstallScreen; verify the existing install_fee authoring note still reads truthfully (it already promises 'the screen won't offer those') — no change unless it drifted. Only add authoring prose for exports that a DESIGNER sets in the Inspector; internal guards (C6/C8/C31 clamps, liveness gates) get code comments in their own findings, not AUTHORING_GUIDE entries.
- **test:** none: authoring-doc prose, no runtime surface.
- **docs:** This finding IS the doc update.
- **depends_on:** F-HYG-6
- **verification:** code-only · **effort:** S
- **risk:** Only edit for exports that actually landed — if C63 keeps the value on AudioSettings vs NpcAudioSettings, name the file the code fix chose (confirm from that finding's diff, don't guess). Renaming pickup_held_collision_layer (C67) is optional in its finding; only do the (b) rg-sweep if the rename actually happened.

#### F-HYG-4 · [low] Refuted-finding breadcrumbs so the next review doesn't re-flag intentional design
- **files:** docs/CURRENT_ARCHITECTURE.md
- **change:** The 2026-07-11 review's Refuted section (doc lines 423-447) is the canonical 'verified non-issue' ledger; keep it intact — do NOT delete docs/audits/architecture-review-2026-07-11.md. To stop re-flagging, add ONE consolidated 'Reviewed & intentionally-not-a-bug' bullet list to CURRENT_ARCHITECTURE.md's Current Design Risks section (line 226+), each a single line naming the site + why it's deliberate, drawn from the refuted list: corpse discovery is distance-only sensing (not a view cone) by design; can_see_node uses raw range in the target-agnostic path on purpose; the three interaction verbs' ~450-line duplication is accepted (takedown ray keeps collide_with_areas=false, so a TALK_LAYER mask would be redundant); GOAP/drop-in components writing host privates is the accepted Node-typed posture; Merchant.sell has no Zorkmids guard because the coin row is value 0.0 and disabled (no pump); Settings._ready seeds from authored GameSettings values BEFORE load_settings (does not overwrite tuning); pre-existing Group/InputManager string literals are out of scope for the registry convention (scoped to new code); DialogueLine END/CONTINUE sentinels are deliberately mirrored in the @tool plugin to avoid a class_name dep. Prefer this SINGLE doc ledger over scattering comments across 8 shared files (which would needlessly touch npc.gd/merchant/perception/Settings and collide with other themes). EXCEPTION: C40/C48 were only partially refuted (kept as cleanup) — do not list them as non-issues.
- **test:** none: doc prose.
- **docs:** This finding IS the doc update (Current Design Risks addition).
- **verification:** code-only · **effort:** S
- **risk:** Do not scatter inline comments into shared god-files (npc.gd, Settings.gd, merchant) for this — the consolidated doc ledger avoids collision with Themes 2/3/7/8 that are actively editing those files. Keep each line true to the review's exact refutation wording; an over-broad 'this is fine' invites a real future regression to hide behind it.

### Batch 21 — Editor-closed verify pass: --import, sidecars, GUT, playtest, asset bakes
**Fixes:** F-HYG-1, F-C52  
**Why here:** The single editor-closed session that owns everything the import lock blocks. F-HYG-1 (depends F-HYG-5) runs --import ONCE to generate every new test's .gd.uid, then the missing-uid grep sweep before staging. F-C52 (bake chip_chess_visualizer icon) needs a WINDOWED renderer (not headless) and a post-bake reimport. This pass also collects: game.tscn reimport (P0-1/C56), neutral_wildlife.tres uid regen (C64), the full GUT suite (all off-tree tests authored above), and every playtest-gated behavior.  
**Verify:** This IS the deferred-verification batch. Sequence: close editor → --headless --path . --import (sidecars + reimport) → missing-uid grep → windowed icon bake (C52) → reopen to import PNG → full GUT run (assert_lte/gte, no Player/NPC _ready) → editor reload for warnings → playtest the modal/death/cutscene/hotbar/chess/faction seams.

#### F-HYG-1 · [high] Enumerate & generate .gd.uid sidecars for every NEW script the batch creates
- **files:** tests/test_new_game_scene_contract.gd, tests/test_chess_wager_settlement.gd, tests/test_ability_registry_map.gd, tests/test_save_migration_v2.gd, docs/audits/editor_closed_session_runbook.md
- **change:** Every new .gd file this remediation batch creates needs a committed .gd.uid sidecar; the editor is OPEN now so --import cannot run — sidecars MUST be generated in the single editor-closed pass and are NOT hand-written (uid:// strings must be engine-generated per memory 'gd-uid-hand-written-invalid'). Anticipated NEW scripts (all are GUT tests other planners will spec; source-side fixes are methods/statics on EXISTING files — CharacterStats.restamp_derived on character_stats.gd, Explosion.instantiate_recovering on the existing explosion script, InputManager._modal_screens rewrite, chess settlement helper on chess_match.gd — so they need NO new sidecar): (1) tests/test_new_game_scene_contract.gd [P0-1], (2) tests/test_chess_wager_settlement.gd [C25], (3) tests/test_ability_registry_map.gd [C21], (4) tests/test_save_migration_v2.gd [C43]. Any additional new .gd a planner introduces joins this list. Mechanism: after ALL code edits land, in the editor-closed pass run `& "C:\Users\dalla\bin\godot.cmd" --headless --path . --import` ONCE (this both compiles-imports and writes each missing sibling .gd.uid), then verify none are missing with `git status --porcelain | grep '\.gd$' | while read _ f; do [ -f "$f.uid" ] || echo MISSING:"$f"; done` and `git add` each new .gd together with its generated .gd.uid. Record this enumerated list + the two commands as a 'New-script sidecar checklist' section in docs/audits/editor_closed_session_runbook.md so the editor-closed operator does not miss one.
- **test:** none: sidecar generation is itself the editor-closed step; the tests being created are the deliverables of other findings.
- **docs:** docs/audits/editor_closed_session_runbook.md — add 'New-script sidecar checklist' (the 4 anticipated files + the import + missing-uid grep).
- **depends_on:** F-HYG-5
- **verification:** gut-editor-closed · **effort:** S
- **risk:** The new-script list is only as complete as the other planners' specs at batch-assembly time — the runbook checklist's grep sweep (not the hand list) is the real guard, so it must run before commit. Do NOT hand-write any uid:// (malformed → crash on preload). Never run --import while the editor is still open (yields empty PackedScenes; memory 'dont-import-with-editor-open').

#### F-C52 · [low] Bake the missing inventory icon for chip_chess_visualizer
- **files:** resources/items/chip_chess_visualizer.tres, resources/icons/chip_chess_visualizer.png
- **change:** chip_chess_visualizer is the only chip with no baked resources/icons/<id>.png (the other 6 chips have resources/icons/chip_*.png), so its grid tile falls to the heavier live-mesh render (Item.icon resolution order: authored icon -> baked resources/icons/<id>.png -> live mesh; item.gd L30-34). No code change: run the CLI baker, which bakes EVERY res://resources/items/*.tres to resources/icons/<id>.png from its world_model:
	godot --path "C:/Users/dalla/3D RPG/rpg" -s scripts/tools/bake_item_icons.gd
Run WINDOWED (NOT --headless — the bake needs a renderer; the tool push_errors + quits under headless). It writes resources/icons/chip_chess_visualizer.png (plus a fresh .import sidecar on next editor import). Optionally narrow to just this item if re-baking all is undesirable, but the tool is idempotent (re-bakes identical PNGs for the rest).
- **test:** none: asset-bake, not code. Verify by opening the inventory in a playtest and confirming the Board Visualizer chip shows a baked 2D tile, not a live-mesh render or a glyph.
- **docs:** None (docs already describe the baker in scripts/tools/bake_item_icons.gd header + AUTHORING_GUIDE icon pipeline).
- **verification:** playtest-gated · **effort:** S
- **risk:** HARD ENVIRONMENT BLOCKER: the editor is OPEN (import lock) and the baker needs a windowed renderer; freshly written PNGs also need the editor to import them before the game can load them. This step CANNOT be completed now — it is editor-closed session work: close the editor, run the windowed baker, reopen the editor to import the PNG. Until then chip_chess_visualizer keeps rendering via live mesh (functional, just heavier).

## Deferred to editor-closed pass
- --import ONCE to generate .gd.uid sidecars for every new test script (F-HYG-1): test_new_game_contract.gd, test_pickup_ray_liveness.gd, test_chess_wager.gd, test_world_frozen.gd, test_npc_flee_targetless.gd, test_npc_give_up_barks.gd, test_ragdoll_scene.gd, test_pickups.gd, test_ps1_applier.gd, plus any refuse/suspend test — followed by the missing-uid grep sweep before staging
- game.tscn reimport after the P0-1 starting_unlocks empty + C56 stupidbody deletion (Batch 2); re-grep to confirm both persisted
- neutral_wildlife.tres uid REGENERATION via editor reimport (F-C64) — the header only gains a fresh uid:// on next import
- chip_chess_visualizer icon bake (F-C52) — needs a WINDOWED (not headless) renderer, then reopen editor to import the PNG
- Full GUT suite run (all off-tree tests authored across Batches 2-19; GUT 9.x assert_lte/gte, no Player/NPC _ready in units)
- Playtest-gated behaviors: P0-3b overflow strip hit-test; P0-4 in-tree noise/corpse scan; T2-1/2/3/4 input-routing + muzzle-flash-after-await + death-mid-carry save-fold; T5-1/2 live wager+install money flows; C34/C34b damage immunity + preserved DoT durations; C6/C7/C5/C36 in-tree NPC behaviors; C31 Pet/Claim probe range; C33 auto-aggro; C38 F5/F9 under overlay; C41 d-pad under name box; C49 PS1 render; C42 hotbar reload behavior
- Editor reload to surface GDScript warnings (headless -s emits none)

## Open decisions (defaults taken)
- Branch vs main: F-HYG-6 recommends staying on main with per-file path staging (314 dirty files + open editor make a branch unsafe). Confirm before starting.
- game.tscn text-edits (P0-1 empty starting_unlocks + C56 stupidbody removal, Batch 2): apply via the text tool with the editor CLOSED, OR have the user perform them in the editor's Scene dock (dock deletion auto-drops the orphaned ext_resource and avoids buffered-revert). The .tscn is almost certainly open in the running editor — genuine either/or.
- M1 autoload split (GameState decomposition): F-REM-DOCS formally PARKS it as a standalone future commit (trigger fired 3x without riding along; tree heavily dirty; L-effort atomic change). Confirm park-now vs execute — no functional blocker remains since M15/B-F40 landed cleanly.
- P0-3b overflow strip (Batch 5, L-effort, playtest-gated hit-test): ship the code now and validate in the editor-closed pass, OR hold it until a session where the strip layout/click can be playtested before commit. F-P0-3a already drains the common corpse-overflow case, so 3b is only needed for a stack too big for an empty grid.